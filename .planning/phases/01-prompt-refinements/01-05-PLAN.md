---
phase: 01-prompt-refinements
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/prompts/gestor-ciclo.prompt.ts
autonomous: true
requirements:
  - PRMT-16
  - PRMT-17

must_haves:
  truths:
    - "O prompt de gestor-ciclo exige que decisoes incluam trade-off explicito ou rejeicao de alternativa"
    - "O prompt de gestor-ciclo exige minimo 1 aprendizado — array vazio nao e mais aceito"
  artifacts:
    - path: "src/main/prompts/gestor-ciclo.prompt.ts"
      provides: "buildGestorCicloPrompt com regras de decisao e aprendizado endurecidas"
      contains: "trade-off"
  key_links:
    - from: "gestor-ciclo.prompt.ts"
      to: "GestorCicloAIResult.decisoes e aprendizados"
      via: "prompt rules"
      pattern: "trade-off\|aprendizado"
---

<objective>
Implementar PRMT-16 e PRMT-17 no gestor-ciclo.prompt.ts: exigir que cada decisao registrada inclua trade-off explicito ou direcao contraria a alternativa, e tornar o campo aprendizados obrigatorio com minimo 1 item.

Purpose: "Continuamos como está" sem trade-off nao e uma decisao — e ruido. E sempre ha algo que o gestor aprendeu sobre o time, o produto ou o processo no ciclo, mesmo que pequeno.
Output: buildGestorCicloPrompt com regras endurecidas para decisoes e aprendizados, sem alterar a interface GestorCicloAIResult.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/main/prompts/gestor-ciclo.prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar PRMT-16 e PRMT-17 no gestor-ciclo.prompt.ts</name>
  <read_first>
    - src/main/prompts/gestor-ciclo.prompt.ts — ler o arquivo completo, especialmente as regras de decisoes e aprendizados
    - tasks/backlog.md — secao R6, tasks T-R6.28 e T-R6.29
  </read_first>
  <files>src/main/prompts/gestor-ciclo.prompt.ts</files>
  <action>
**PRMT-16 — Decisao exige trade-off (T-R6.28):**

Localizar a regra atual de `decisoes`:
```
- decisoes: pode ser array vazio [] se não houve decisão clara do gestor.
```

Substituir por regra expandida que define o que conta como decisao:
```
- decisoes: lista de decisões ou direcionamentos com substância. Uma "decisão" só é registrada se inclui PELO MENOS UM dos seguintes:
  (a) um trade-off explícito — "escolhemos A em detrimento de B porque X"
  (b) rejeição de uma alternativa — "descartamos a opção Y pois Z"
  (c) uma posição tomada frente à ambiguidade ou pressão — "mantivemos o prazo apesar de X porque Y"
  NÃO registre como decisão: atualizações de status ("sprint foi concluída"), planos futuros sem posicionamento ("vamos ver"), ou frases neutras sem escolha ("continuamos como está").
  Array vazio [] apenas se genuinamente não houve nenhuma decisão com substância neste artefato.
```

**PRMT-17 — Aprendizado obrigatorio (T-R6.29):**

Localizar a regra atual de `aprendizados`:
```
- aprendizados: pode ser array vazio [].
```

Substituir por regra que torna o campo obrigatorio:
```
- aprendizados: OBRIGATÓRIO — mínimo 1 item. Sempre há algo que o gestor aprendeu, observou ou pode aplicar — sobre dinâmica do time, capacidade de estimativa, processo de decisão, necessidade de alguém, sinal que apareceu, dependência identificada. Cada item descreve: o QUE foi observado/aprendido + o IMPACTO ou consequência para gestão futura. Nunca retorne array vazio.
  Exemplos de itens válidos (mesmo quando o evento foi operacional):
  - "Ana demonstrou capacidade de liderança técnica sob pressão — pode ser candidate a Tech Lead no próximo ciclo"
  - "Estimativas do time para tasks de infraestrutura são consistentemente 2x acima do realizado — padrão a calibrar nos próximos plannings"
  - "Decisão de atrasar o lançamento gerou alivio no time — sinaliza que estavam sob pressão não comunicada antes"
```

Nao alterar a interface `GestorCicloAIResult` nem a funcao `renderGestorCicloMarkdown`.
  </action>
  <verify>
    <automated>grep -n "trade-off\|aprendizados\|decisoes\|OBRIGATÓRIO" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/gestor-ciclo.prompt.ts | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "trade-off" src/main/prompts/gestor-ciclo.prompt.ts` retorna ao menos 2 matches (na regra e no exemplo)
    - `grep "rejeição de uma alternativa" src/main/prompts/gestor-ciclo.prompt.ts` retorna ao menos 1 match
    - `grep "NÃO registre como decisão" src/main/prompts/gestor-ciclo.prompt.ts` retorna ao menos 1 match (contraste negativo)
    - `grep "OBRIGATÓRIO" src/main/prompts/gestor-ciclo.prompt.ts` retorna ao menos 1 match na regra de aprendizados
    - `grep "mínimo 1 item" src/main/prompts/gestor-ciclo.prompt.ts` retorna ao menos 1 match na regra de aprendizados
    - `grep "Nunca retorne array vazio" src/main/prompts/gestor-ciclo.prompt.ts` retorna ao menos 1 match
    - A regra anterior "pode ser array vazio" NAO deve mais aparecer em aprendizados: `grep "pode ser array vazio" src/main/prompts/gestor-ciclo.prompt.ts` deve retornar 0 (ou apenas para acoes_minhas, nao aprendizados)
  </acceptance_criteria>
  <done>PRMT-16 e PRMT-17: decisoes exigem trade-off/alternativa rejeitada; aprendizados sao obrigatorios com minimo 1 item e exemplos concretos</done>
</task>

</tasks>

<verification>
```bash
grep -c "trade-off" src/main/prompts/gestor-ciclo.prompt.ts
grep -c "OBRIGATÓRIO" src/main/prompts/gestor-ciclo.prompt.ts
grep -c "mínimo 1 item" src/main/prompts/gestor-ciclo.prompt.ts
```
Todos devem retornar >= 1.
</verification>

<success_criteria>
- PRMT-16: regra de decisoes com definicao clara do que e uma decisao (trade-off/alternativa rejeitada/posicao frente a pressao) e o que NAO e uma decisao
- PRMT-17: aprendizados obrigatorios com minimo 1 item, exemplos concretos de aprendizados validos mesmo em eventos operacionais
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-prompt-refinements/01-05-SUMMARY.md`
</output>
