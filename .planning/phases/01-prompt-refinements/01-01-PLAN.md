---
phase: 01-prompt-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/prompts/ingestion.prompt.ts
  - src/main/prompts/1on1-deep.prompt.ts
  - src/main/prompts/cerimonia-sinal.prompt.ts
  - src/main/prompts/compression.prompt.ts
autonomous: true
requirements:
  - PRMT-01
  - PRMT-02
  - PRMT-03
  - PRMT-04
  - PRMT-05
  - PRMT-06
  - PRMT-07

must_haves:
  truths:
    - "O prompt de ingestion registra pessoas_esperadas_ausentes para planning/retro/daily"
    - "O prompt de ingestion detecta early stagnation nos primeiros 3 meses com janela explícita de 90 dias"
    - "O prompt 1on1-deep exige 2+ entradas de historico para usar tendencia_emocional: deteriorando"
    - "O prompt de cerimonia calibra participacao minima por tipo (daily/planning/retro/review)"
    - "O prompt de cerimonia calibra indicador_saude por cargo/nivel (Staff silencioso != Junior silencioso)"
    - "O prompt de compression define ponto resolvido unicamente: strikethrough OU contradicao por evidencia"
    - "O prompt de compression preserva conquistas no formato titulo — outcome na consolidacao"
  artifacts:
    - path: "src/main/prompts/ingestion.prompt.ts"
      provides: "Regras de pessoas_esperadas_ausentes e early stagnation no buildIngestionPrompt"
      contains: "pessoas_esperadas_ausentes"
    - path: "src/main/prompts/1on1-deep.prompt.ts"
      provides: "Regra de tendencia deteriorando com contagem1on1s"
      contains: "deteriorando"
    - path: "src/main/prompts/cerimonia-sinal.prompt.ts"
      provides: "Expectativas por tipo de cerimonia e calibracao por cargo"
      contains: "pessoaCargo"
    - path: "src/main/prompts/compression.prompt.ts"
      provides: "Definicao de ponto resolvido e formato de conquistas"
      contains: "strikethrough"
  key_links:
    - from: "ingestion.prompt.ts"
      to: "IngestionAIResult.pessoas_esperadas_ausentes"
      via: "field in interface"
      pattern: "pessoas_esperadas_ausentes"
    - from: "1on1-deep.prompt.ts"
      to: "contagem1on1s parameter"
      via: "template literal"
      pattern: "contagem1on1s"
---

<objective>
Auditar e confirmar PRMT-01 a PRMT-07 nos prompts de ingestion, 1on1-deep, cerimonia-sinal e compression. A maioria desses requisitos foi implementada nos commits v3 — esta tarefa verifica a implementacao, corrige quaisquer gaps menores identificados na leitura e formaliza evidencias para o checklist.

Purpose: Garantir que os prompts de menor escopo tenham implementacao completa e sem ambiguidades antes de marcar os requisitos como fechados.
Output: Quatro arquivos de prompt auditados e corrigidos onde necessario, com evidencias grep-verificaveis para cada PRMT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main/prompts/ingestion.prompt.ts
@src/main/prompts/1on1-deep.prompt.ts
@src/main/prompts/cerimonia-sinal.prompt.ts
@src/main/prompts/compression.prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar e corrigir ingestion.prompt.ts (PRMT-01, PRMT-02)</name>
  <read_first>
    - src/main/prompts/ingestion.prompt.ts — ler o arquivo completo para ver estado atual das regras
    - tasks/backlog.md — secao R6, tasks T-R6.2 e T-R6.4 para criterios de aceite exatos
  </read_first>
  <files>src/main/prompts/ingestion.prompt.ts</files>
  <action>
Ler o arquivo e verificar se as seguintes regras ja estao implementadas. Se nao estiverem, implementa-las cirurgicamente.

**PRMT-01 — pessoas_esperadas_ausentes (T-R6.2):**
Verificar que a regra no prompt diz: "slugs de pessoas do time cadastrado que DEVERIAM estar presentes neste tipo de evento mas estavam ausentes. Use o contexto para inferir: se é planning e o owner de um item de backlog não apareceu, registre. Se é retro e um membro fixo do time não apareceu, registre. Em reuniões ad-hoc ou 1:1, use array vazio — ausência não é informativa nesses contextos."
Se diferir ou estiver ausente, adicionar/corrigir esta regra exata na secao de regras do prompt.

**PRMT-02 — early stagnation (T-R6.4):**
Verificar que a regra de alerta_estagnacao diz explicitamente: "Janela de avaliação mínima: 2 artefatos nos últimos 90 dias sem nenhum sinal de crescimento. NÃO aguarde 6+ meses para reportar — early stagnation (0-3 meses) é igualmente relevante."
Se diferir ou estiver ausente, adicionar/corrigir esta regra exata.

Nao alterar nenhum outro campo do arquivo. Preservar todas as interfaces TypeScript.
  </action>
  <verify>
    <automated>grep -n "pessoas_esperadas_ausentes" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/ingestion.prompt.ts | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "pessoas_esperadas_ausentes" src/main/prompts/ingestion.prompt.ts` retorna pelo menos 3 matches (na regra, no JSON esperado e na interface)
    - `grep "90 dias" src/main/prompts/ingestion.prompt.ts` retorna ao menos 1 match na regra de alerta_estagnacao
    - `grep "0-3 meses" src/main/prompts/ingestion.prompt.ts` retorna ao menos 1 match
    - `grep "NÃO aguarde" src/main/prompts/ingestion.prompt.ts` retorna ao menos 1 match
    - Arquivo compila sem erros TypeScript: `npx tsc --noEmit src/main/prompts/ingestion.prompt.ts 2>&1 | head -5` nao retorna erros de tipo
  </acceptance_criteria>
  <done>PRMT-01 e PRMT-02 com regras explicitas e verificaveis no buildIngestionPrompt</done>
</task>

<task type="auto">
  <name>Task 2: Auditar 1on1-deep.prompt.ts (PRMT-03) e cerimonia-sinal.prompt.ts (PRMT-04, PRMT-05)</name>
  <read_first>
    - src/main/prompts/1on1-deep.prompt.ts — ler campo tendencia_emocional e contagem1on1s
    - src/main/prompts/cerimonia-sinal.prompt.ts — ler secao de expectativas por tipo e calibracao por cargo
    - tasks/backlog.md — secao R6, tasks T-R6.9, T-R6.11 e T-R6.14
  </read_first>
  <files>src/main/prompts/1on1-deep.prompt.ts, src/main/prompts/cerimonia-sinal.prompt.ts</files>
  <action>
**1on1-deep.prompt.ts — PRMT-03 (T-R6.9):**
Verificar que a regra de tendencia_emocional diz: "deteriorando: SOMENTE quando há evidência de piora NESTE 1:1 E na última entrada do histórico de saúde. Um único 1:1 ruim nunca é suficiente para 'deteriorando' — use 'novo_sinal' nesses casos. REGRA ADICIONAL: se o histórico de saúde tem menos de 2 entradas de 1:1 (contagem atual: ${contagem1on1s}), NUNCA use 'deteriorando' — use 'novo_sinal' obrigatoriamente."
Se diferir, corrigir para incluir: (a) exigencia de evidencia em 2+ entradas consecutivas, (b) checagem explicita de contagem1on1s < 2 forcando novo_sinal.

**cerimonia-sinal.prompt.ts — PRMT-04 (T-R6.11):**
Verificar que existe secao de "Expectativas mínimas por tipo de cerimônia" com regras distintas para:
- daily: "qualquer atualização de status — mesmo breve — conta como participação. Silêncio total é sinal."
- planning: "espera-se que a pessoa comente sobre itens do backlog relevantes ao seu trabalho, faça estimativas ou perguntas."
- retro: "espera-se pelo menos uma perspectiva própria. Concordar com tudo sem contribuição original é sinal de baixo engajamento."
- review: "espera-se demonstração técnica ou comentário técnico fundamentado."
Se diferir ou ausente, adicionar/corrigir antes da secao "Por categoria".

**cerimonia-sinal.prompt.ts — PRMT-05 (T-R6.14):**
Verificar que a regra de indicador_saude inclui calibracao por cargo/nivel: "CALIBRE pelo cargo/nível (${pessoaCargo}): para pessoas em nível sênior ou de liderança, o bar de 'verde' é mais alto — espera-se contribuição ativa, tomada de posição e influência nas discussões. Para níveis júnior/pleno em início de trajetória, participação mais passiva com sinais de aprendizado pode ser 'verde'."
Se diferir ou ausente, adicionar/corrigir na descricao do campo indicador_saude.
  </action>
  <verify>
    <automated>grep -n "contagem1on1s" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/1on1-deep.prompt.ts | head -5 && grep -n "pessoaCargo" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/cerimonia-sinal.prompt.ts | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "contagem1on1s" src/main/prompts/1on1-deep.prompt.ts` retorna ao menos 2 matches (no parametro e na regra)
    - `grep "novo_sinal" src/main/prompts/1on1-deep.prompt.ts` retorna ao menos 2 matches
    - `grep "NUNCA use" src/main/prompts/1on1-deep.prompt.ts` retorna ao menos 1 match
    - `grep "daily" src/main/prompts/cerimonia-sinal.prompt.ts` retorna match com regra de participacao minima
    - `grep "pessoaCargo" src/main/prompts/cerimonia-sinal.prompt.ts` retorna ao menos 2 matches (no parametro e na regra de indicador_saude)
    - `grep "sênior ou de liderança" src/main/prompts/cerimonia-sinal.prompt.ts` retorna ao menos 1 match
  </acceptance_criteria>
  <done>PRMT-03, PRMT-04, PRMT-05 com regras explicitas e calibradas por cargo/nivel e historico de 1:1s</done>
</task>

<task type="auto">
  <name>Task 3: Auditar compression.prompt.ts (PRMT-06, PRMT-07)</name>
  <read_first>
    - src/main/prompts/compression.prompt.ts — ler regras de pontos_ativos e conquistas
    - tasks/backlog.md — secao R6, tasks T-R6.20 e T-R6.21
  </read_first>
  <files>src/main/prompts/compression.prompt.ts</files>
  <action>
**PRMT-06 — definicao de ponto resolvido (T-R6.20):**
Verificar que a regra de pontos_ativos diz: "Um ponto é 'resolvido' se: (a) está riscado com ~~strikethrough~~ no bloco, OU (b) foi listado em `pontos_resolvidos` de algum artefato recente."
Esta e a definicao harmonizada — strikethrough OU contradicao por evidencia (via pontos_resolvidos). Adicionar texto explicativo se necessario: "NUNCA remova pontos sem um desses dois critérios explícitos."

**PRMT-07 — conquistas formato titulo + outcome (T-R6.21):**
Verificar que a regra de conquistas especifica formato obrigatorio: `"[TÍTULO DO QUÊ] — [OUTCOME mensurável ou qualitativo]"` com exemplos como "Liderou migração Kafka — latência -40%, sem downtime".
Se o formato obrigatorio estiver presente mas sem exemplos suficientes, adicionar mais um exemplo: "Implementou feature de pagamentos — redução de 30% em erros de checkout reportados pelos clientes".
Verificar que a regra diz "Preserve os últimos 60 dias verbatim" para nao comprimir conquistas recentes.
  </action>
  <verify>
    <automated>grep -n "strikethrough" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/compression.prompt.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "strikethrough" src/main/prompts/compression.prompt.ts` retorna ao menos 2 matches (definicao e exemplo)
    - `grep "pontos_resolvidos" src/main/prompts/compression.prompt.ts` retorna ao menos 1 match na regra de pontos_ativos
    - `grep "TÍTULO DO QUÊ" src/main/prompts/compression.prompt.ts` retorna ao menos 1 match com formato obrigatorio
    - `grep "60 dias" src/main/prompts/compression.prompt.ts` retorna ao menos 1 match na regra de conquistas
    - `grep "OUTCOME" src/main/prompts/compression.prompt.ts` retorna ao menos 1 match no formato de conquistas
  </acceptance_criteria>
  <done>PRMT-06 e PRMT-07 com definicao unificada de ponto resolvido e formato preservado de conquistas</done>
</task>

</tasks>

<verification>
Ao final das 3 tasks, executar:
```bash
grep -c "pessoas_esperadas_ausentes" src/main/prompts/ingestion.prompt.ts
grep -c "90 dias" src/main/prompts/ingestion.prompt.ts
grep -c "contagem1on1s" src/main/prompts/1on1-deep.prompt.ts
grep -c "pessoaCargo" src/main/prompts/cerimonia-sinal.prompt.ts
grep -c "strikethrough" src/main/prompts/compression.prompt.ts
```
Todos devem retornar >= 1.
</verification>

<success_criteria>
- PRMT-01: `pessoas_esperadas_ausentes` com regra de inferencia de ausencia em planning/retro/daily e array vazio em ad-hoc
- PRMT-02: `alerta_estagnacao` com janela explicita de 90 dias e mencao de early stagnation 0-3 meses
- PRMT-03: `tendencia_emocional: "deteriorando"` exige 2+ entradas E contagem1on1s >= 2
- PRMT-04: Expectativas minimas por tipo de cerimonia (daily/planning/retro/review) definidas separadamente
- PRMT-05: `indicador_saude` calibrado por cargo/nivel com barra mais alta para seniors
- PRMT-06: Definicao harmonizada — ponto resolvido = strikethrough OU pontos_resolvidos
- PRMT-07: Conquistas preservam formato "[TITULO] — [OUTCOME]", ultimos 60 dias verbatim
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-prompt-refinements/01-01-SUMMARY.md`
</output>
