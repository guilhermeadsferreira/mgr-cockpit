---
phase: 01-prompt-refinements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/prompts/autoavaliacao.prompt.ts
autonomous: true
requirements:
  - PRMT-11
  - PRMT-12

must_haves:
  truths:
    - "O prompt de autoavaliacao calibra o eixo 'como_demonstrei_valores' por tipo de role (manager vs IC)"
    - "O prompt de autoavaliacao captura desafios_observados como campo obrigatorio quando ha evidencia"
    - "A interface AutoavaliacaoAIResult inclui o campo desafios_observados"
  artifacts:
    - path: "src/main/prompts/autoavaliacao.prompt.ts"
      provides: "buildAutoavaliacaoPrompt com calibracao por role e campo desafios_observados"
      contains: "desafios_observados"
  key_links:
    - from: "autoavaliacao.prompt.ts"
      to: "AutoavaliacaoAIResult.desafios_observados"
      via: "interface field"
      pattern: "desafios_observados"
---

<objective>
Implementar PRMT-11 e PRMT-12 no autoavaliacao.prompt.ts: calibrar os valores de lideranca por tipo de role (manager vs IC) e adicionar o campo desafios_observados como obrigatorio quando ha evidencia de dificuldades no periodo.

Purpose: A autoavaliacao atual e generica — um Staff IC avalia os mesmos eixos que um Engineering Manager, o que gera outputs desconectados da realidade de cada papel. Desafios reconhecidos sao essenciais para coaching e crescimento.
Output: buildAutoavaliacaoPrompt com calibracao por role e interface atualizada com desafios_observados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/main/prompts/autoavaliacao.prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calibrar como_demonstrei_valores por tipo de role (PRMT-11)</name>
  <read_first>
    - src/main/prompts/autoavaliacao.prompt.ts — ler o arquivo completo para ver interface, parametros e regras atuais
    - tasks/backlog.md — secao R6, task T-R6.23 para criterio exato
  </read_first>
  <files>src/main/prompts/autoavaliacao.prompt.ts</files>
  <action>
**Passo 1 — Verificar parametro managerRole:**
O parametro `managerRole: string` ja existe na interface `AutoavaliacaoPromptParams`. Este campo e usado na identificacao do gestor no prompt ("assistente de ${managerName}${role}") mas nao calibra os eixos de avaliacao.

**Passo 2 — Adicionar instrucao de calibracao no prompt:**
Na secao de regras de `como_demonstrei_valores`, adicionar instrucao de calibracao logo antes da regra atual do campo:

```
- como_demonstrei_valores: mínimo 2 itens. Inclua evidências específicas (situações reais). CALIBRE pelo tipo de papel:
  - Se managerRole indica GESTÃO (ex: Engineering Manager, Tech Lead, Head of, Director): foque em comportamentos de gestão e liderança organizacional — "como facilitei o crescimento do time", "como tomei decisões sob incerteza", "como alinhei expectativas com stakeholders", "como criei ambiente de segurança psicológica", "como influenciei a direção técnica do produto".
  - Se managerRole indica IC (ex: Software Engineer, Senior Engineer, Staff Engineer, Principal, Arquiteto): foque em comportamentos de impacto técnico e colaboração — "como contribuí para qualidade técnica", "como compartilhei conhecimento", "como elevei o padrão do time com code reviews", "como demonstrei ownership de problemas complexos", "como influenciei decisões de arquitetura".
  - Nunca use os mesmos eixos genéricos para ambos os casos — o valor de um IC e o valor de um manager são fundamentalmente diferentes.
```

Localizar a linha `- como_demonstrei_valores: mínimo 2 itens. Inclua evidências específicas (situações reais).` e substituir por esta versao expandida.
  </action>
  <verify>
    <automated>grep -n "managerRole\|GESTÃO\|IC\|como_demonstrei_valores" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/autoavaliacao.prompt.ts | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "GESTÃO\|Engineering Manager" src/main/prompts/autoavaliacao.prompt.ts` retorna ao menos 1 match na secao de como_demonstrei_valores
    - `grep "IC\|Staff Engineer\|Software Engineer" src/main/prompts/autoavaliacao.prompt.ts` retorna ao menos 1 match na secao de como_demonstrei_valores
    - `grep "Nunca use os mesmos eixos" src/main/prompts/autoavaliacao.prompt.ts` retorna ao menos 1 match
    - A funcao buildAutoavaliacaoPrompt ainda compila (interface AutoavaliacaoPromptParams inalterada)
  </acceptance_criteria>
  <done>PRMT-11: como_demonstrei_valores calibrado por tipo de role com instrucoes especificas para manager e IC</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar campo desafios_observados a autoavaliacao (PRMT-12)</name>
  <read_first>
    - src/main/prompts/autoavaliacao.prompt.ts — ler interface AutoavaliacaoAIResult e regras de campos
    - tasks/backlog.md — secao R6, task T-R6.24 para criterio exato
  </read_first>
  <files>src/main/prompts/autoavaliacao.prompt.ts</files>
  <action>
**Passo 1 — Adicionar campo na interface AutoavaliacaoAIResult:**
Localizar a interface `AutoavaliacaoAIResult` e adicionar o campo:
```typescript
export interface AutoavaliacaoAIResult {
  o_que_fiz_e_entreguei:    string[]
  como_demonstrei_valores:  string[]
  desafios_observados:      string[]  // areas de dificuldade, incerteza ou crescimento pendente
  como_me_vejo_no_futuro:   string
}
```

**Passo 2 — Adicionar campo no JSON esperado do prompt:**
Na secao de JSON esperado do prompt, adicionar `"desafios_observados": ["string"]` entre `como_demonstrei_valores` e `como_me_vejo_no_futuro`:
```json
{
  "o_que_fiz_e_entreguei": ["string — entrega concreta com contexto e impacto"],
  "como_demonstrei_valores": ["string — comportamento ou ação que reflete valores de liderança"],
  "desafios_observados": ["string — área de dificuldade ou crescimento pendente com evidência"],
  "como_me_vejo_no_futuro": "string — parágrafo de 3 a 5 frases sobre aspirações e próximos passos"
}
```

**Passo 3 — Adicionar regra do campo na secao de regras:**
Apos a regra de `como_demonstrei_valores`, adicionar:
```
- desafios_observados: OBRIGATÓRIO quando há evidência de dificuldades, incertezas ou areas de crescimento no período. Array vazio APENAS se não há absolutamente nenhum sinal de desafio nos artefatos — caso raro. Cada item descreve: [ÁREA DE DIFICULDADE] + [EVIDÊNCIA observada no período] + [IMPACTO ou consequência]. Exemplos:
  - "Gestão de prioridades sob pressão: em 3 artefatos do período, sinalizei sobrecarga mas não consegui delegar ou escalar antes do prazo — resultado: entregas de menor qualidade em semanas de pico"
  - "Comunicação ascendente: não antecipei riscos de entrega ao meu gestor até a última semana, mesmo com sinais de atraso desde o meio do ciclo"
  Nunca use frases genéricas como "preciso melhorar comunicação" sem evidência comportamental observada.
```

**Passo 4 — Atualizar renderAutoavaliacaoMarkdown:**
Adicionar secao de desafios no render markdown, apos a secao "Como demonstrei valores":
```typescript
`## Desafios Observados`,
``,
...result.desafios_observados.map((item) => `- ${item}`),
``,
```
Se `desafios_observados` estiver vazio, omitir a secao (adicionar condicional).
  </action>
  <verify>
    <automated>grep -n "desafios_observados" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/autoavaliacao.prompt.ts | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "desafios_observados" src/main/prompts/autoavaliacao.prompt.ts` retorna ao menos 4 matches (interface, JSON template, regra, render function)
    - `grep "OBRIGATÓRIO quando há evidência" src/main/prompts/autoavaliacao.prompt.ts` retorna ao menos 1 match
    - `grep "Nunca use frases genéricas" src/main/prompts/autoavaliacao.prompt.ts` retorna ao menos 1 match
    - `grep "Gestão de prioridades" src/main/prompts/autoavaliacao.prompt.ts` retorna ao menos 1 match como exemplo
    - `grep "Desafios Observados" src/main/prompts/autoavaliacao.prompt.ts` retorna ao menos 1 match no render markdown
    - Arquivo TypeScript valido — interface AutoavaliacaoAIResult com campo desafios_observados: string[]
  </acceptance_criteria>
  <done>PRMT-12: desafios_observados adicionado na interface, prompt e render — obrigatorio quando ha evidencia de dificuldade</done>
</task>

</tasks>

<verification>
```bash
grep -c "desafios_observados" src/main/prompts/autoavaliacao.prompt.ts
grep -c "GESTÃO\|Engineering Manager" src/main/prompts/autoavaliacao.prompt.ts
grep -c "OBRIGATÓRIO quando há evidência" src/main/prompts/autoavaliacao.prompt.ts
```
Todos devem retornar >= 1.
</verification>

<success_criteria>
- PRMT-11: como_demonstrei_valores com instrucoes distintas para manager (gestao/lideranca organizacional) vs IC (impacto tecnico/colaboracao)
- PRMT-12: desafios_observados como campo obrigatorio quando ha evidencia — na interface, no JSON esperado, nas regras e no render markdown
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-prompt-refinements/01-03-SUMMARY.md`
</output>
