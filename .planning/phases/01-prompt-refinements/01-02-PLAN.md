---
phase: 01-prompt-refinements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/prompts/cycle.prompt.ts
autonomous: true
requirements:
  - PRMT-08
  - PRMT-09
  - PRMT-10

must_haves:
  truths:
    - "linha_do_tempo aceita entre 5-10 eventos, com IA decidindo densidade por significancia"
    - "evolucao_frente_ao_cargo ancora em expectativas do nivel/cargo especifico"
    - "evidencias_promovibilidade quando flag=nao lista gaps com comportamento observado concreto"
  artifacts:
    - path: "src/main/prompts/cycle.prompt.ts"
      provides: "Regras de linha_do_tempo, evolucao_frente_ao_cargo e evidencias_promovibilidade"
      contains: "5 e 10 eventos"
  key_links:
    - from: "cycle.prompt.ts"
      to: "CycleAIResult.evidencias_promovibilidade"
      via: "prompt rule"
      pattern: "comportamento observado"
---

<objective>
Auditar cycle.prompt.ts para confirmar PRMT-08 e PRMT-09 (ja implementados nos commits v3) e implementar PRMT-10: evidencias de promovibilidade quando flag="nao" devem incluir gaps com comportamento observado concreto, nao apenas "nao ha evidencias".

Purpose: Fechar o gap de qualidade mais importante do cycle prompt — o caso mais frequente na pratica e exatamente "nao" ou "avaliar", e esses casos precisam de evidencias comportamentais acionaveis.
Output: cycle.prompt.ts com regra explicita de "comportamento observado" nas evidencias de promovibilidade negativa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/main/prompts/cycle.prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar linha_do_tempo e evolucao_frente_ao_cargo (PRMT-08, PRMT-09)</name>
  <read_first>
    - src/main/prompts/cycle.prompt.ts — ler regras de linha_do_tempo e evolucao_frente_ao_cargo completas
    - tasks/backlog.md — secao R6, tasks T-R6.17 e T-R6.18
  </read_first>
  <files>src/main/prompts/cycle.prompt.ts</files>
  <action>
**PRMT-08 — linha_do_tempo flexivel (T-R6.17):**
Verificar que a regra de linha_do_tempo diz: "entre 5 e 10 eventos-chave do período em ordem cronológica. Mínimo de 5 itens — se o período for rico, inclua até 10."
A regra atual pode dizer apenas "10 eventos". Se assim for, corrigir para: "entre 5 e 10 eventos-chave do período em ordem cronológica. Mínimo de 5 itens — se o período for rico, inclua até 10. Inclua entregas, marcos, mudanças, incidentes relevantes e momentos de virada (feedbacks recebidos, reconhecimentos, bloqueios superados)."

**PRMT-09 — expectativas benchmarked por cargo (T-R6.18):**
Verificar que a regra de evolucao_frente_ao_cargo diz: "OBRIGATÓRIO: ancore em expectativas do nível (use o campo 'cargo' do perfil como referência). Exemplo: 'Para um Sênior, espera-se autonomia técnica e influência no time — neste ciclo demonstrou X e Y, mas ainda depende de Z para atingir plenamente o nível esperado.'"
Se diferir ou exemplo estiver ausente, corrigir para incluir este texto exato.
  </action>
  <verify>
    <automated>grep -n "5 e 10" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/cycle.prompt.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "5 e 10" src/main/prompts/cycle.prompt.ts` retorna ao menos 1 match na regra de linha_do_tempo
    - `grep "Mínimo de 5" src/main/prompts/cycle.prompt.ts` retorna ao menos 1 match
    - `grep "ancore em expectativas" src/main/prompts/cycle.prompt.ts` retorna ao menos 1 match
    - `grep "Para um Sênior" src/main/prompts/cycle.prompt.ts` retorna ao menos 1 match com exemplo concreto
  </acceptance_criteria>
  <done>PRMT-08 e PRMT-09 com regras de densificacao flexivel e benchmarking por cargo confirmadas</done>
</task>

<task type="auto">
  <name>Task 2: Implementar evidencias de promovibilidade com comportamento observado (PRMT-10)</name>
  <read_first>
    - src/main/prompts/cycle.prompt.ts — ler a regra atual de evidencias_promovibilidade completa
    - tasks/backlog.md — secao R6, task T-R6.19 para criterio exato
  </read_first>
  <files>src/main/prompts/cycle.prompt.ts</files>
  <action>
**PRMT-10 — evidencias nunca triviais (T-R6.19):**

Localizar a regra atual de "evidencias_promovibilidade" no prompt. Ela provavelmente diz: "Nunca retorne array vazio — sempre há algo a dizer."

Substituir ou enriquecer essa regra com:

```
- "evidencias_promovibilidade": 3–5 bullets de evidência concreta que sustentam o flag_promovibilidade. Cada bullet deve ser autônomo e citável no fórum: descreva um fato específico (entrega, comportamento, feedback de terceiro) com data ou contexto.
  QUANDO flag = "nao": liste as lacunas que precisam ser DEMONSTRADAS antes de uma promoção. Cada gap deve incluir: (a) o comportamento esperado para o nível, (b) o que foi observado (ou não observado) no ciclo, (c) evidência comportamental concreta. NUNCA use linguagem vaga como "falta experiência" ou "não está pronto" sem evidência. Exemplos de formato correto:
  - "Liderança técnica (esperada para Senior): não liderou nenhuma iniciativa de arquitetura de ponta a ponta no ciclo — iniciativas de refactoring foram fragmentadas e sem ownership claro"
  - "Influência no time (esperada para Staff): feedback de 1:1 e cerimônias indica que colega X não é buscado para decisões técnicas pelo time — padrão observado em 3+ artefatos"
  QUANDO flag = "condicionado_a": liste o que já foi demonstrado (evidências positivas) E o que ainda falta (gap comportamental específico).
  Nunca retorne array vazio — sempre há algo a dizer.
```

Substituir apenas a regra de evidencias_promovibilidade. Nao alterar outros campos.
  </action>
  <verify>
    <automated>grep -n "comportamento observado\|comportamento esperado\|comportamento" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/cycle.prompt.ts | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "flag = .nao." src/main/prompts/cycle.prompt.ts` retorna ao menos 1 match na regra de evidencias_promovibilidade
    - `grep "comportamento esperado" src/main/prompts/cycle.prompt.ts` retorna ao menos 1 match
    - `grep "NUNCA use linguagem vaga" src/main/prompts/cycle.prompt.ts` retorna ao menos 1 match
    - `grep "Liderança técnica (esperada para Senior)" src/main/prompts/cycle.prompt.ts` retorna ao menos 1 match como exemplo de formato
    - `grep "nunca retorne array vazio" src/main/prompts/cycle.prompt.ts` (case-insensitive) retorna ao menos 1 match
    - Arquivo compila sem erros: `npx tsc --noEmit --skipLibCheck 2>&1 | grep "cycle.prompt" | head -5` nao retorna erros
  </acceptance_criteria>
  <done>PRMT-10: evidencias_promovibilidade com regra explicita de comportamento observado para flag=nao e condicionado_a</done>
</task>

</tasks>

<verification>
```bash
grep -c "5 e 10" src/main/prompts/cycle.prompt.ts
grep -c "ancore em expectativas" src/main/prompts/cycle.prompt.ts
grep -c "comportamento esperado" src/main/prompts/cycle.prompt.ts
grep -c "flag = .nao." src/main/prompts/cycle.prompt.ts
```
Todos devem retornar >= 1.
</verification>

<success_criteria>
- PRMT-08: linha_do_tempo aceita 5-10 eventos com regra de densidade por significancia
- PRMT-09: evolucao_frente_ao_cargo ancora em expectativas de cargo/nivel com exemplo concreto
- PRMT-10: evidencias_promovibilidade com flag=nao exige comportamento esperado + observado + evidencia comportamental concreta — sem fillers
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-prompt-refinements/01-02-SUMMARY.md`
</output>
