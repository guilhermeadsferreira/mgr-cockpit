---
phase: 01-prompt-refinements
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/prompts/gemini-preprocessing.prompt.ts
autonomous: true
requirements:
  - PRMT-13
  - PRMT-14
  - PRMT-15

must_haves:
  truths:
    - "detectPreprocessingMode determina o modo pelo conteudo da transcricao (num_speakers) quando disponivel, nao apenas pelo filename"
    - "O modo full captura observacoes de tom emocional (frustracao, excitacao, tensao) em secao opcional"
    - "GeminiPreprocessingResult inclui speaker_confidence como metadata (alta/media/baixa)"
  artifacts:
    - path: "src/main/prompts/gemini-preprocessing.prompt.ts"
      provides: "detectPreprocessingMode por conteudo, emotional content em full mode, speaker_confidence"
      contains: "speaker_confidence"
  key_links:
    - from: "gemini-preprocessing.prompt.ts detectPreprocessingMode"
      to: "num_speakers detection logic"
      via: "content analysis"
      pattern: "num_speakers\|speaker_confidence"
---

<objective>
Implementar PRMT-13, PRMT-14 e PRMT-15 no gemini-preprocessing.prompt.ts: deteccao de modo por conteudo (num_speakers) ao inves de filename, captura de conteudo emocional em full mode, e campo speaker_confidence como metadata no resultado.

Purpose: A deteccao por filename e fragil — "Sync com Ana" pode ser 1:1 ou standup. Conteudo emocional em retros/plannings e perdido no modo full. Speaker confidence permite que o executor avalie se atribuicoes de fala sao confiaveis.
Output: detectPreprocessingMode atualizada com analise de conteudo, buildFullPrompt com secao de tone observations, e GeminiPreprocessingResult com speaker_confidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/main/prompts/gemini-preprocessing.prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deteccao de modo por conteudo e speaker_confidence no resultado (PRMT-13, PRMT-15)</name>
  <read_first>
    - src/main/prompts/gemini-preprocessing.prompt.ts — ler detectPreprocessingMode, GeminiPreprocessingResult e buildGeminiPreprocessingPrompt completos
    - tasks/backlog.md — secao R6, tasks T-R6.25 e T-R6.27
  </read_first>
  <files>src/main/prompts/gemini-preprocessing.prompt.ts</files>
  <action>
**PRMT-13 — Mode detection por conteudo (T-R6.25):**

A funcao `detectPreprocessingMode(fileName: string)` atualmente usa apenas o filename. Substituir pela seguinte logica que aceita conteudo opcional:

```typescript
/**
 * Detecta o modo de pré-processamento adequado.
 * Prioridade: análise de conteúdo (quando disponível) > nome do arquivo.
 *
 * Análise de conteúdo: conta speakers distintos nas primeiras 500 chars.
 * - 1-2 speakers → 'light' (1:1 ou conversa bilateral)
 * - 3+ speakers → 'full' (cerimônia coletiva)
 */
export function detectPreprocessingMode(
  fileName: string,
  contentPreview?: string,
): GeminiPreprocessingMode {
  // Análise de conteúdo tem prioridade se disponível
  if (contentPreview) {
    const preview = contentPreview.slice(0, 500)
    // Conta speakers distintos: padrões como "Nome:", "Speaker 1:", "[Nome]"
    const speakerMatches = preview.match(/^[A-ZÀ-Ú][a-zA-ZÀ-ú\s]{1,30}:/gm) || []
    const uniqueSpeakers = new Set(speakerMatches.map(s => s.trim().toLowerCase()))
    if (uniqueSpeakers.size >= 3) return 'full'
    if (uniqueSpeakers.size <= 2 && uniqueSpeakers.size > 0) return 'light'
    // Se não detectou speakers pelo padrão, cai para análise por filename
  }

  // Fallback: análise por nome de arquivo (comportamento original)
  const lower = fileName.toLowerCase()
  if (
    lower.includes('1on1') ||
    lower.includes('1-on-1') ||
    lower.includes('one-on-one') ||
    lower.includes('one on one') ||
    /\b1[_\s]?:\s?1\b/.test(lower)
  ) {
    return 'light'
  }
  return 'full'
}
```

Substituir a implementacao atual de `detectPreprocessingMode` por esta versao. Assinatura muda de `(fileName: string)` para `(fileName: string, contentPreview?: string)` — compativel com chamadas existentes pois segundo parametro e opcional.

**PRMT-15 — Speaker confidence (T-R6.27):**

Adicionar campo `speaker_confidence` na interface `GeminiPreprocessingResult`:

```typescript
export interface GeminiPreprocessingResult {
  texto_limpo: string
  metadados: {
    data_reuniao?: string
    participantes: string[]
    duracao_minutos?: number
    speaker_confidence: 'alta' | 'media' | 'baixa'  // confianca na atribuicao de fala
  }
  estatisticas: {
    tokens_removidos: number
    percentual_economia: number
  }
}
```

Criterio de `speaker_confidence`:
- "alta": cada fala tem atribuicao clara por nome ou label consistente ao longo da transcricao
- "media": maioria das falas identificada, mas algumas ambiguidades
- "baixa": transcricao sem identificacao de speakers ou com atribuicao inconsistente

Adicionar instrucao de `speaker_confidence` nos dois prompts (light e full), na secao de JSON esperado:

No prompt light, adicionar ao metadados:
```
"speaker_confidence": "alta|media|baixa — quão confiável é a atribuição de fala. 'alta': cada fala tem nome/label claro e consistente. 'media': maioria identificada, algumas ambiguidades. 'baixa': sem identificação ou com atribuição inconsistente."
```

No prompt full, idem.

Atualizar a funcao `isValidResult` para nao exigir o campo (backward-compat — campo pode ser ausente em respostas antigas):
O campo `speaker_confidence` e optional na validacao — nao adicionar verificacao obrigatoria em `isValidResult`.
  </action>
  <verify>
    <automated>grep -n "speaker_confidence\|contentPreview\|num_speakers" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/gemini-preprocessing.prompt.ts | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "contentPreview" src/main/prompts/gemini-preprocessing.prompt.ts` retorna ao menos 2 matches (parametro e uso na funcao)
    - `grep "speaker_confidence" src/main/prompts/gemini-preprocessing.prompt.ts` retorna ao menos 4 matches (interface, metadados, e nos dois prompts)
    - `grep "uniqueSpeakers\|speakerMatches" src/main/prompts/gemini-preprocessing.prompt.ts` retorna ao menos 2 matches na logica de deteccao
    - A assinatura de detectPreprocessingMode aceita segundo argumento opcional: `grep "contentPreview?: string" src/main/prompts/gemini-preprocessing.prompt.ts` retorna ao menos 1 match
    - `grep "fallback\|Fallback" src/main/prompts/gemini-preprocessing.prompt.ts` retorna ao menos 1 match (comentario de fallback para filename)
  </acceptance_criteria>
  <done>PRMT-13 e PRMT-15: detectPreprocessingMode usa conteudo com fallback para filename; GeminiPreprocessingResult inclui speaker_confidence</done>
</task>

<task type="auto">
  <name>Task 2: Capturar conteudo emocional em full mode (PRMT-14)</name>
  <read_first>
    - src/main/prompts/gemini-preprocessing.prompt.ts — ler buildFullPrompt completa, especialmente a secao de "PRESERVAR" e o formato de texto_limpo
    - tasks/backlog.md — secao R6, task T-R6.26 para criterio exato
  </read_first>
  <files>src/main/prompts/gemini-preprocessing.prompt.ts</files>
  <action>
**PRMT-14 — Emotional content em full mode (T-R6.26):**

O `buildFullPrompt` atualmente na secao de PRESERVAR nao menciona conteudo emocional — ele e descartado junto com "conversas pessoais" e "preenchidores".

**Passo 1 — Adicionar regra de PRESERVAR no full mode:**
Na secao "### 2. PRESERVAR", adicionar ao final da lista:
```
- Sinais emocionais coletivos relevantes: frustração com processo, excitação com resultado, tensão entre membros, resistência a decisão — quando esses sinais aparecem em reunião coletiva, são dados de gestão de times (ex: "time todo demonstrou resistência à proposta de migração", "clima de frustração durante retrospectiva ao discutir prazo")
```

**Passo 2 — Adicionar secao "Observações de Tom" no formato do texto_limpo:**
Na secao "### 4. FORMATO DO TEXTO_LIMPO", adicionar nova secao opcional ao final do template markdown:

```
## Observações de Tom (opcional — incluir apenas quando houver sinais emocionais coletivos relevantes)
- [Sinal emocional observado]: [Contexto e momento da reunião]
```

Exemplos a incluir na instrucao:
```
Exemplos:
- "Frustração generalizada: ao discutir estimativas de prazo, maioria do time expressou resistência — possível desalinhamento de expectativas com stakeholders"
- "Excitação coletiva: demonstração de novo produto gerou energia alta no time, com 3+ membros pedindo para participar do projeto"
- "Tensão pontual: discussão sobre responsabilidade pelo bug de producao gerou atrito entre dois membros — resolvido ao final da retro"
Se não houver sinais emocionais coletivos relevantes, OMITIR a seção completamente.
```

**Passo 3 — Nao alterar buildLightPrompt:**
O modo light ja tem regras de preservacao emocional (secao "### 2. PRESERVAR INTEGRALMENTE"). Nao modificar.
  </action>
  <verify>
    <automated>grep -n "Observações de Tom\|Tom\|emocional\|frustração\|sinais emocionais" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/prompts/gemini-preprocessing.prompt.ts | grep -v "buildLightPrompt\|modo LIGHT\|LIGHT\|Modo LIGHT" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Observações de Tom" src/main/prompts/gemini-preprocessing.prompt.ts` retorna ao menos 1 match no full mode
    - `grep "sinais emocionais coletivos" src/main/prompts/gemini-preprocessing.prompt.ts` retorna ao menos 1 match na secao de PRESERVAR do full mode
    - `grep "Frustração generalizada" src/main/prompts/gemini-preprocessing.prompt.ts` retorna ao menos 1 match como exemplo
    - `grep "OMITIR a seção completamente" src/main/prompts/gemini-preprocessing.prompt.ts` retorna ao menos 1 match (instrucao de omitir quando nao ha sinais)
    - buildLightPrompt NAO foi alterada: `grep -c "Observações de Tom" src/main/prompts/gemini-preprocessing.prompt.ts` retorna 1 (apenas no full mode, nao no light)
  </acceptance_criteria>
  <done>PRMT-14: full mode captura sinais emocionais coletivos em secao opcinal "Observacoes de Tom" — preservado sem inventar</done>
</task>

</tasks>

<verification>
```bash
grep -c "speaker_confidence" src/main/prompts/gemini-preprocessing.prompt.ts
grep -c "contentPreview" src/main/prompts/gemini-preprocessing.prompt.ts
grep -c "Observações de Tom" src/main/prompts/gemini-preprocessing.prompt.ts
grep -c "sinais emocionais coletivos" src/main/prompts/gemini-preprocessing.prompt.ts
```
Todos devem retornar >= 1.
</verification>

<success_criteria>
- PRMT-13: detectPreprocessingMode analisa primeiras 500 chars para contar speakers unicos; 3+ speakers = full, 1-2 = light; filename como fallback
- PRMT-14: buildFullPrompt preserva sinais emocionais coletivos e gera secao "Observacoes de Tom" opcional
- PRMT-15: GeminiPreprocessingResult.metadados inclui speaker_confidence com enum alta/media/baixa
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-prompt-refinements/01-04-SUMMARY.md`
</output>
