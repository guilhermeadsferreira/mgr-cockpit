---
phase: 999.14-sustentacao-alertas-proativos
plan: 02
type: execute
wave: 2
depends_on:
  - 999.14-01
files_modified:
  - src/renderer/src/components/Sidebar.tsx
  - src/renderer/src/views/SustentacaoView.tsx
autonomous: true
requirements:
  - ALRT-04
  - ALRT-05

must_haves:
  truths:
    - "Quando ha alertas ativos, o item Sustentacao na sidebar exibe um dot vermelho visivel"
    - "Quando os alertas zeram (apos refresh), o badge some sem recarregar o app"
    - "Ao abrir SustentacaoView com alertas ativos, um banner aparece logo abaixo do header listando os alertas"
    - "O banner some automaticamente quando um refresh retorna snapshot sem alertas"
    - "Quando nao ha alertas, a sidebar nao exibe badge e a SustentacaoView nao exibe banner"
  artifacts:
    - path: "src/renderer/src/components/Sidebar.tsx"
      provides: "badge dot vermelho no item Sustentacao quando alertas.length > 0"
      contains: "alertas"
    - path: "src/renderer/src/views/SustentacaoView.tsx"
      provides: "banner inline de alertas abaixo do header"
      contains: "AlertasBanner"
  key_links:
    - from: "Sidebar.tsx"
      to: "window.api.sustentacao.getData()"
      via: "useEffect no mount para carregar count de alertas"
      pattern: "sustentacao.getData"
    - from: "SustentacaoView.tsx (snapshot.alertas)"
      to: "banner condicional"
      via: "snapshot.alertas.length > 0 antes do conteudo principal"
      pattern: "snapshot.alertas"
---

<objective>
Exibir badge dot vermelho no item Sustentacao da sidebar quando ha alertas ativos, e banner inline no topo da SustentacaoView listando os alertas — ambos derivados de snapshot.alertas do Plan 01.

Purpose: Fechar o ciclo de alertas — logica calculada no backend (Plan 01) precisa ser visivel para o gestor.
Output: Sidebar com badge condicional, SustentacaoView com banner de alertas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.14-sustentacao-alertas-proativos/999.14-CONTEXT.md
@.planning/phases/999.14-sustentacao-alertas-proativos/999.14-01-SUMMARY.md

<interfaces>
<!-- Sidebar.tsx — estado e pattern de badge existente -->
```typescript
// Sidebar.tsx — badge pattern ja existe para Demandas (openDemandasCount)
// NavItem interface ja tem badge?: number
// NavBtn ja renderiza badge com estilo red-dim quando item.badge !== undefined

interface NavItem {
  id: ViewName
  label: string
  icon: React.ReactNode
  badge?: number
}

// Uso atual do badge (referencia):
{ id: 'eu', label: 'Eu', icon: <User size={14} />, badge: openDemandasCount > 0 ? openDemandasCount : undefined }
```

<!-- SupportBoardSnapshot.alertas — disponivel apos Plan 01 -->
```typescript
// ipc.ts — apos Plan 01
export interface SustentacaoAlerta {
  tipo: 'breach_crescente' | 'ticket_envelhecendo' | 'fila_crescendo' | 'spike_incidente'
  mensagem: string
  severidade: 'critico' | 'atencao'
}

// SupportBoardSnapshot.alertas: SustentacaoAlerta[]
```

<!-- window.api — preload existente -->
```typescript
// preload/index.ts — sustentacao ja exposto
window.api.sustentacao.getData()   // Promise<SupportBoardSnapshot | null>
window.api.sustentacao.refresh()   // Promise<SupportBoardSnapshot | null>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge dot vermelho no item Sustentacao da Sidebar</name>
  <files>src/renderer/src/components/Sidebar.tsx</files>
  <read_first>
    - src/renderer/src/components/Sidebar.tsx (inteiro — entender estado existente, pattern de badge do item Eu, e onde inserir o useEffect de sustentacao)
  </read_first>
  <action>
O badge existente no item "Eu" usa `badge?: number` (numero de demandas abertas). Para Sustentacao, usar o mesmo campo com valor `alertasCount > 0 ? alertasCount : undefined` — o dot ja sera renderizado pelo NavBtn existente.

**Decisao visual (per Claude's Discretion):** usar o mesmo componente de badge numerico existente — mostra o numero de alertas (ex: "2"), com a mesma cor red-dim ja estabelecida. Consistencia com o padrao existente.

**Mudancas em Sidebar.tsx:**

1. Adicionar import do tipo (ao topo, junto dos outros imports de ipc.ts):
```typescript
import type { Demanda, SupportBoardSnapshot } from '../types/ipc'
```

2. Adicionar estado de alertas logo apos o estado de `openDemandasCount`:
```typescript
const [alertasCount, setAlertasCount] = useState(0)
```

3. Adicionar useEffect para carregar alertas de sustentacao no mount (apos o useEffect de demandas):
```typescript
  useEffect(() => {
    async function loadAlertasSustentacao() {
      try {
        const data = await window.api.sustentacao.getData() as SupportBoardSnapshot | null
        setAlertasCount(data?.alertas?.length ?? 0)
      } catch { /* sustentacao pode nao estar configurada */ }
    }
    loadAlertasSustentacao()
    // Atualizar badge quando sustentacao for refreshada
    window.addEventListener('sustentacao:refreshed', loadAlertasSustentacao)
    return () => window.removeEventListener('sustentacao:refreshed', loadAlertasSustentacao)
  }, [])
```

4. Atualizar o item `sustentacao` em `navItems` para incluir badge:
```typescript
// Antes:
{ id: 'sustentacao', label: 'Sustentacao', icon: <Wrench size={14} /> },

// Depois:
{ id: 'sustentacao', label: 'Sustentacao', icon: <Wrench size={14} />, badge: alertasCount > 0 ? alertasCount : undefined },
```

**Nota:** O evento `sustentacao:refreshed` sera disparado pela SustentacaoView apos cada refresh bem-sucedido (implementado na Task 2). Se o evento nao existir (plano futuro), o badge simplesmente nao atualiza em tempo real — sem crash.
  </action>
  <verify>
    grep -n "alertasCount\|sustentacao:refreshed" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/components/Sidebar.tsx
  </verify>
  <acceptance_criteria>
    - `alertasCount` estado presente no componente Sidebar
    - useEffect que chama `window.api.sustentacao.getData()` e seta `alertasCount` presente
    - Item `sustentacao` em `navItems` tem `badge: alertasCount > 0 ? alertasCount : undefined`
    - Listener para evento `sustentacao:refreshed` registrado e limpo no cleanup do useEffect
    - Import de `SupportBoardSnapshot` adicionado
    - Nenhum outro item de navItems alterado
  </acceptance_criteria>
  <done>Badge numerico aparece no item Sustentacao da sidebar quando alertasCount > 0; some quando alertasCount === 0</done>
</task>

<task type="auto">
  <name>Task 2: Banner de alertas inline na SustentacaoView</name>
  <files>src/renderer/src/views/SustentacaoView.tsx</files>
  <read_first>
    - src/renderer/src/views/SustentacaoView.tsx (inteiro — entender estrutura de return, onde inserir o banner, e como snapshot.alertas esta disponivel)
  </read_first>
  <action>
Adicionar banner inline logo abaixo do bloco `{error && ...}` e antes do conteudo principal (cards de metricas), visivel apenas quando `snapshot.alertas.length > 0`.

**Decisao visual (per Claude's Discretion):**
- Container: background `var(--red-dim)`, border `1px solid rgba(184,64,64,0.25)`, border-radius 8, padding 12 16, margin 0 40 16
- Titulo: "Alertas ativos" com icone `AlertTriangle` (lucide-react, size 14), cor `var(--red)`, font-weight 600
- Lista: cada alerta em linha separada com bullet e mensagem, tamanho 12.5px, cor `var(--text-secondary)`
- Icone diferenciado por severidade: `critico` → `AlertCircle` (vermelho), `atencao` → `AlertTriangle` (amarelo/accent)
- Ordem: alertas `critico` primeiro, depois `atencao`

**Mudancas em SustentacaoView.tsx:**

1. Adicionar imports de lucide-react no topo (junto com `RefreshCw, Loader2, Wrench`):
```typescript
import { RefreshCw, Loader2, Wrench, AlertTriangle, AlertCircle } from 'lucide-react'
```

2. Adicionar import do tipo apos Plan 01:
```typescript
import type { SupportBoardSnapshot, SustentacaoHistoryEntry, SustentacaoAlerta } from '../types/ipc'
```

3. Adicionar funcao helper `AlertasBanner` como componente local (antes do `export function SustentacaoView`):
```typescript
function AlertasBanner({ alertas }: { alertas: SustentacaoAlerta[] }) {
  if (alertas.length === 0) return null

  const sorted = [...alertas].sort((a, b) => {
    // critico primeiro
    if (a.severidade === 'critico' && b.severidade !== 'critico') return -1
    if (b.severidade === 'critico' && a.severidade !== 'critico') return 1
    return 0
  })

  return (
    <div style={{
      margin: '0 40px 16px',
      background: 'var(--red-dim)',
      border: '1px solid rgba(184,64,64,0.25)',
      borderRadius: 8,
      padding: '12px 16px',
    }}>
      <div style={{
        display: 'flex', alignItems: 'center', gap: 6,
        fontSize: 12.5, fontWeight: 600,
        color: 'var(--red)',
        marginBottom: sorted.length > 0 ? 8 : 0,
        fontFamily: 'var(--font)',
      }}>
        <AlertTriangle size={13} />
        {sorted.length === 1 ? '1 alerta ativo' : `${sorted.length} alertas ativos`}
      </div>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 4 }}>
        {sorted.map((alerta, i) => (
          <div key={i} style={{
            display: 'flex', alignItems: 'flex-start', gap: 6,
            fontSize: 12.5, color: 'var(--text-secondary)',
            lineHeight: 1.45,
          }}>
            {alerta.severidade === 'critico'
              ? <AlertCircle size={12} style={{ color: 'var(--red)', flexShrink: 0, marginTop: 1 }} />
              : <AlertTriangle size={12} style={{ color: 'var(--accent)', flexShrink: 0, marginTop: 1 }} />
            }
            {alerta.mensagem}
          </div>
        ))}
      </div>
    </div>
  )
}
```

4. No render principal (dentro do `return` do `SustentacaoView`), inserir `<AlertasBanner>` logo apos o bloco de erro `{error && ...}` e antes dos cards de metricas:

```tsx
{/* Alertas proativos — visivel quando snapshot tem alertas ativos */}
{snapshot.alertas && snapshot.alertas.length > 0 && (
  <AlertasBanner alertas={snapshot.alertas} />
)}
```

5. Apos cada refresh bem-sucedido, disparar evento para atualizar o badge da sidebar. No final do `handleRefresh` (dentro do `try`, apos `setSnapshot(data)`):
```typescript
      setSnapshot(data)
      // Notificar sidebar para atualizar badge
      window.dispatchEvent(new CustomEvent('sustentacao:refreshed'))
```

**Nota:** `snapshot.alertas` pode ser `undefined` se o cache ja existia antes deste plan ser deployado (dados antigos sem o campo). Usar `snapshot.alertas ?? []` para seguranca defensiva no banner.
  </action>
  <verify>
    grep -n "AlertasBanner\|alertas\|sustentacao:refreshed" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/views/SustentacaoView.tsx
  </verify>
  <acceptance_criteria>
    - `AlertasBanner` componente local definido antes de `SustentacaoView`
    - `AlertTriangle` e `AlertCircle` importados de lucide-react
    - `SustentacaoAlerta` importado de `../types/ipc`
    - `<AlertasBanner alertas={snapshot.alertas ?? []} />` (ou com verificacao) presente no JSX de SustentacaoView
    - Banner renderizado APOS o bloco de erro e ANTES dos cards de metricas
    - `window.dispatchEvent(new CustomEvent('sustentacao:refreshed'))` chamado no `handleRefresh` apos sucesso
    - `sorted` ordena `critico` antes de `atencao`
    - Sem alertas (`alertas.length === 0`): `AlertasBanner` retorna null (sem elemento renderizado)
  </acceptance_criteria>
  <done>Banner aparece no topo da SustentacaoView quando ha alertas; some apos refresh sem alertas; sidebar recebe evento para atualizar badge</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Badge dot numerico na sidebar (item Sustentacao) + banner inline de alertas na SustentacaoView.
    Logica de calculo dos 4 tipos de alerta no backend (Plan 01).
  </what-built>
  <how-to-verify>
    1. Abrir o app e navegar para Sustentacao
    2. Se ha breach > 2 vs 7 dias atras, ticket com >10d (para threshold 5d), ou fila subindo 3 dias: badge numerico deve aparecer na sidebar
    3. Ao abrir SustentacaoView, banner deve listar os alertas ativos com icones e mensagens
    4. Clicar em "Atualizar" — se condicoes melhoraram, banner some e badge zera
    5. Se nao ha dados de history.json (primeiros dias de uso), app nao crasha — apenas sem alertas

    Para testar sem dados reais:
    - Verificar no console do app (DevTools) se snapshot retornado pelo IPC contem campo `alertas`
    - `window.api.sustentacao.getData().then(d => console.log(d?.alertas))` no console do renderer
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit

# Badge na sidebar
grep -n "alertasCount\|sustentacao:refreshed" src/renderer/src/components/Sidebar.tsx

# Banner na view
grep -n "AlertasBanner\|alertas" src/renderer/src/views/SustentacaoView.tsx | head -20

# Build limpo
npx tsc --noEmit 2>&1 | head -30
```
</verification>

<success_criteria>
- Sidebar carrega alertasCount via sustentacao.getData() no mount
- Badge numerico aparece no item Sustentacao quando alertasCount > 0
- AlertasBanner renderiza acima dos cards quando snapshot.alertas.length > 0
- Refresh dispara evento sustentacao:refreshed para atualizar badge da sidebar
- TypeScript compila sem erros
- Comportamento defensivo: snapshot.alertas undefined nao causa crash (usar ?? [])
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/999.14-sustentacao-alertas-proativos/999.14-02-SUMMARY.md`
</output>
