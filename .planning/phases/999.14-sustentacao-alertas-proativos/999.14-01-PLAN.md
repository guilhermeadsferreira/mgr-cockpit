---
phase: 999.14-sustentacao-alertas-proativos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/types/ipc.ts
  - src/main/external/SupportBoardClient.ts
  - src/main/index.ts
autonomous: true
requirements:
  - ALRT-01
  - ALRT-02
  - ALRT-03

must_haves:
  truths:
    - "Ao chamar sustentacao:get-data ou sustentacao:refresh, o snapshot retornado inclui campo alertas[] com zero ou mais alertas calculados"
    - "Breach crescente (breachCount atual > breachCount de 7 dias atras por +2 ou mais) gera alerta tipo breach_crescente"
    - "Ticket aberto por mais de 2x o SLA do seu tipo gera alerta tipo ticket_envelhecendo"
    - "ticketsAbertos subindo 3 ou mais dias consecutivos no history gera alerta tipo fila_crescendo"
    - "3 ou mais tickets do mesmo tipo+label criados nas ultimas 2h gera alerta tipo spike_incidente"
    - "Quando history.json nao existe ou tem menos de 2 entradas, alertas D-04 e D-06 ficam silenciosos (sem crash)"
  artifacts:
    - path: "src/renderer/src/types/ipc.ts"
      provides: "SustentacaoAlerta tipo + SupportBoardSnapshot.alertas campo"
      contains: "SustentacaoAlerta"
    - path: "src/main/external/SupportBoardClient.ts"
      provides: "calcularAlertas() exportada"
      exports: ["calcularAlertas"]
    - path: "src/main/index.ts"
      provides: "IPC handler inclui alertas no snapshot retornado"
      contains: "calcularAlertas"
  key_links:
    - from: "src/main/index.ts (fetchAndCacheSustentacao)"
      to: "calcularAlertas() em SupportBoardClient"
      via: "chamada direta apos montar snapshot + passar issues[] raw"
      pattern: "calcularAlertas"
    - from: "src/main/external/SupportBoardClient.ts"
      to: "SustentacaoAlerta em ipc.ts"
      via: "import type { SustentacaoAlerta }"
      pattern: "SustentacaoAlerta"
---

<objective>
Definir o tipo SustentacaoAlerta, implementar calcularAlertas() com as 4 condicoes de alerta, e integrar no IPC handler para que o snapshot retornado ao renderer ja carregue os alertas calculados.

Purpose: Base de dados para o badge na sidebar e banner na SustentacaoView — sem esta logica nao ha dados para exibir.
Output: SustentacaoAlerta em ipc.ts, calcularAlertas() em SupportBoardClient.ts, snapshot.alertas preenchido no IPC handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.14-sustentacao-alertas-proativos/999.14-CONTEXT.md

<interfaces>
<!-- Tipos existentes relevantes — extraidos de src/renderer/src/types/ipc.ts -->

```typescript
// ipc.ts (linhas 155-206) — tipos existentes de sustentacao

export interface SupportTicket {
  key: string
  summary: string
  type: string
  labels: string[]
  assignee: string | null
  ageDias: number
  status: string
  slaBreached: boolean
  recentComments: Array<{ author: string; body: string; created: string }>
}

export interface SupportBoardSnapshot {
  atualizadoEm: string
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  topTipos: Array<{ tipo: string; count: number }>
  topLabels: Array<{ label: string; count: number }>
  ticketsEmBreach: SupportTicket[]
  porAssignee: Record<string, number>
  complianceRate7d: number | null
  complianceRate30d: number | null
  history: SustentacaoHistoryEntry[]
  // NOVO: alertas calculados (a ser adicionado neste plan)
}

export interface SustentacaoHistoryEntry {
  date: string         // YYYY-MM-DD
  fetchedAt: number    // Unix ms
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  breachCount: number
  complianceRate7d: number | null
  complianceRate30d: number | null
}
```

<!-- JiraIssue — disponivel no SupportBoardClient para calcular spike (D-07) -->
```typescript
// src/main/external/JiraClient.ts
export interface JiraIssue {
  key: string
  summary: string
  status: string
  statusCategory: 'new' | 'indeterminate' | 'done'
  type: string
  priority: string
  assignee: string | null
  created: string   // ISO timestamp — usado para filtrar ultimas 2h no spike
  updated: string
  resolved: string | null
  storyPoints: number | null
  labels: string[]
  sprint: string | null
  sprintId: number | null
  blockedSince: string | null
  linkedBlockers: string[]
}
```

<!-- fetchSupportBoardMetrics — retorna snapshot sem alertas, recebe issues[] internamente -->
```typescript
// SupportBoardClient.ts — assinatura atual
export async function fetchSupportBoardMetrics(input: SupportBoardInput): Promise<SupportBoardSnapshot>

export interface SupportBoardInput {
  config: JiraConfig
  projectKey: string
  slaThresholds?: Record<string, number>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipo SustentacaoAlerta + campo alertas no SupportBoardSnapshot</name>
  <files>src/renderer/src/types/ipc.ts</files>
  <read_first>
    - src/renderer/src/types/ipc.ts (inteiro — para inserir no local correto e nao duplicar nada)
  </read_first>
  <action>
Adicionar o tipo `SustentacaoAlerta` e estender `SupportBoardSnapshot` com o campo `alertas`.

**Inserir logo apos a interface `SustentacaoHistoryEntry` (por volta da linha 206):**

```typescript
/** Alerta proativo calculado a cada refresh de dados de sustentacao */
export interface SustentacaoAlerta {
  /** Identificador da condicao que gerou o alerta */
  tipo: 'breach_crescente' | 'ticket_envelhecendo' | 'fila_crescendo' | 'spike_incidente'
  /** Mensagem legivel para exibir no banner */
  mensagem: string
  /** Severidade visual */
  severidade: 'critico' | 'atencao'
}
```

**Adicionar campo `alertas` em `SupportBoardSnapshot`** (ao final da interface, antes do fechamento `}`):

```typescript
  /** Alertas proativos calculados a cada refresh. Array vazio = sem alertas. */
  alertas: SustentacaoAlerta[]
```

Nao alterar nenhum outro campo existente.
  </action>
  <verify>
    grep -n "SustentacaoAlerta" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/types/ipc.ts
  </verify>
  <acceptance_criteria>
    - `export interface SustentacaoAlerta` presente no arquivo
    - Campo `tipo` com union `'breach_crescente' | 'ticket_envelhecendo' | 'fila_crescendo' | 'spike_incidente'`
    - Campo `severidade` com union `'critico' | 'atencao'`
    - Campo `alertas: SustentacaoAlerta[]` presente dentro de `SupportBoardSnapshot`
    - Nenhum campo existente de `SupportBoardSnapshot` removido ou alterado
  </acceptance_criteria>
  <done>ipc.ts compila sem erros de tipo e exporta SustentacaoAlerta + SupportBoardSnapshot.alertas</done>
</task>

<task type="auto">
  <name>Task 2: calcularAlertas() no SupportBoardClient + integracao no IPC handler</name>
  <files>src/main/external/SupportBoardClient.ts, src/main/index.ts</files>
  <read_first>
    - src/main/external/SupportBoardClient.ts (inteiro — entender onde inserir calcularAlertas e como issues[] esta disponivel)
    - src/main/index.ts (linhas 1100-1200 — ver fetchAndCacheSustentacao para o ponto de integracao)
  </read_first>
  <action>
**Em src/main/external/SupportBoardClient.ts:**

1. Adicionar import do tipo novo no topo (junto com os imports existentes de ipc.ts):
```typescript
import type { SupportBoardSnapshot, SupportTicket, SustentacaoAlerta, SustentacaoHistoryEntry } from '../../renderer/src/types/ipc'
```

2. Exportar a funcao `calcularAlertas` logo apos `fetchSupportBoardMetrics` (antes de `fetchSustentacaoForReport`):

```typescript
/** Thresholds fixos — nao configuravel nesta fase (per D-08) */
const ALRT_BREACH_DELTA = 2        // breach crescente: +2 vs 7 dias atras
const ALRT_SLA_MULTIPLIER = 2      // ticket envelhecendo: >2x o SLA do tipo
const ALRT_FILA_DAYS = 3           // fila crescendo: 3 dias consecutivos subindo
const ALRT_SPIKE_COUNT = 3         // spike: 3+ tickets do mesmo tipo+label
const ALRT_SPIKE_WINDOW_MS = 2 * 60 * 60 * 1000  // janela spike: 2h

/**
 * Calcula alertas proativos a partir dos dados disponiveis.
 * Retorna array vazio quando sem alertas — nunca lanca excecao.
 *
 * @param snapshot - snapshot atual (sem alertas ainda)
 * @param history  - entradas diarias do history.json (pode ser [])
 * @param issues   - issues raw do Jira (para calculo de spike por created timestamp)
 * @param slaThresholds - mapa tipo → dias (default 5 para tipos nao mapeados)
 */
export function calcularAlertas(
  snapshot: Omit<SupportBoardSnapshot, 'alertas'>,
  history: SustentacaoHistoryEntry[],
  issues: Array<{ created: string; type: string; labels: string[]; statusCategory: string }>,
  slaThresholds: Record<string, number> = {}
): SustentacaoAlerta[] {
  const alertas: SustentacaoAlerta[] = []

  // D-04: Breach crescente — breach atual vs 7 dias atras por +2 ou mais
  if (history.length >= 2) {
    const sevenDaysMs = 7 * 24 * 60 * 60 * 1000
    const ref = history
      .filter((e) => e.fetchedAt <= Date.now() - sevenDaysMs)
      .sort((a, b) => b.fetchedAt - a.fetchedAt)[0]
    if (ref) {
      const delta = snapshot.ticketsEmBreach.length - ref.breachCount
      if (delta >= ALRT_BREACH_DELTA) {
        alertas.push({
          tipo: 'breach_crescente',
          mensagem: `Breach subiu ${delta} tickets em relacao a 7 dias atras (${ref.breachCount} → ${snapshot.ticketsEmBreach.length})`,
          severidade: delta >= 5 ? 'critico' : 'atencao',
        })
      }
    }
  }

  // D-05: Ticket envelhecendo — aberto por mais de 2x o SLA do seu tipo
  for (const ticket of snapshot.ticketsEmBreach) {
    const threshold = slaThresholds[ticket.type] ?? DEFAULT_SLA_DIAS
    if (ticket.ageDias > ALRT_SLA_MULTIPLIER * threshold) {
      alertas.push({
        tipo: 'ticket_envelhecendo',
        mensagem: `${ticket.key}: ${ticket.ageDias}d aberto (limite ${threshold}d, threshold critico ${ALRT_SLA_MULTIPLIER * threshold}d)`,
        severidade: 'critico',
      })
    }
  }

  // D-06: Fila crescendo — ticketsAbertos subiu N dias consecutivos
  if (history.length >= ALRT_FILA_DAYS) {
    const recent = history
      .slice()
      .sort((a, b) => a.fetchedAt - b.fetchedAt)
      .slice(-(ALRT_FILA_DAYS + 1))  // ultimos N+1 para verificar N consecutivos
    let consecutivos = 0
    for (let i = 1; i < recent.length; i++) {
      if (recent[i].ticketsAbertos > recent[i - 1].ticketsAbertos) {
        consecutivos++
      } else {
        consecutivos = 0
      }
    }
    if (consecutivos >= ALRT_FILA_DAYS) {
      alertas.push({
        tipo: 'fila_crescendo',
        mensagem: `Fila crescendo ha ${consecutivos} dias consecutivos (${recent[recent.length - 1].ticketsAbertos} tickets abertos)`,
        severidade: 'atencao',
      })
    }
  }

  // D-07: Spike de incidente — 3+ tickets do mesmo tipo+label criados nas ultimas 2h
  const cutoffSpike = Date.now() - ALRT_SPIKE_WINDOW_MS
  const abertosRecentes = issues.filter(
    (i) => i.statusCategory !== 'done' && new Date(i.created).getTime() >= cutoffSpike
  )
  const spikeCounts: Record<string, number> = {}
  for (const issue of abertosRecentes) {
    for (const label of issue.labels.length > 0 ? issue.labels : ['_sem_label']) {
      const key = `${issue.type}::${label}`
      spikeCounts[key] = (spikeCounts[key] ?? 0) + 1
    }
  }
  for (const [key, count] of Object.entries(spikeCounts)) {
    if (count >= ALRT_SPIKE_COUNT) {
      const [tipo, label] = key.split('::')
      const labelDesc = label === '_sem_label' ? '' : ` / ${label}`
      alertas.push({
        tipo: 'spike_incidente',
        mensagem: `${count} tickets "${tipo}${labelDesc}" criados nas ultimas 2h — possivel incidente`,
        severidade: 'critico',
      })
    }
  }

  return alertas
}
```

**Em src/main/index.ts — integrar calcularAlertas no fetchAndCacheSustentacao:**

3. Adicionar `calcularAlertas` ao import de SupportBoardClient (junto com `fetchSupportBoardMetrics` e `fetchSustentacaoForReport`).

4. Dentro de `fetchAndCacheSustentacao`, apos a linha onde `data` e montado pelo `fetchSupportBoardMetrics` e ANTES de gravar `board.json`, adicionar:

```typescript
    // Calcular alertas proativos (per D-10, D-11)
    const historyForAlerts = readHistory(join(cacheDir, 'history.json'))
    const alertas = calcularAlertas(data, historyForAlerts, issues, jiraSlaThresholds)
    const dataComAlertas = { ...data, alertas }
```

5. Substituir as referencias a `data` no `writeFileSync(cacheFile, ...)` e no retorno por `dataComAlertas`.

6. Para o **cache hit** (early return do cache): apos ler `cached.data` e antes de retornar, calcular alertas da mesma forma:
```typescript
    // Cache hit — alertas calculados on-the-fly sobre dados cached (sem issues[] raw)
    // Graceful: sem issues raw disponivel no cache, alertas D-07 ficam silenciosos
    const historyData = readHistory(historyFile).slice(-30)
    const alertasCached = calcularAlertas(cached.data, historyData, [], jiraSlaThresholds ?? {})
    return { ...cached.data, history: historyData, alertas: alertasCached }
```

**Nota importante sobre issues[]:** `fetchSupportBoardMetrics` hoje nao exporta o array `issues` raw — ele e local a funcao. Para que `calcularAlertas` receba issues[] para o spike (D-07), ha duas opcoes:
- **Opcao A (preferida):** Criar uma funcao wrapper `fetchSupportBoardMetricsWithIssues` que retorna `{ snapshot, issues }` — sem quebrar a assinatura publica existente.
- **Opcao B:** Passar array vazio `[]` para calcularAlertas nos casos sem issues (D-07 fica silencioso, D-04/D-05/D-06 funcionam normalmente).

Usar **Opcao A** para o fetch real e **Opcao B** para o cache hit — isso garante spike detection quando ha fetch real e graceful degradation em cache hit (alertas D-04/D-05/D-06 funcionam em ambos os casos).

Para implementar Opcao A, criar funcao interna no SupportBoardClient.ts:
```typescript
/** Variante interna que retorna snapshot + issues raw para calcular spike (D-07) */
export async function fetchSupportBoardMetricsWithIssues(
  input: SupportBoardInput
): Promise<{ snapshot: Omit<SupportBoardSnapshot, 'alertas'>; issues: JiraIssue[] }>
```
Extrair a logica atual de `fetchSupportBoardMetrics` para esta funcao e fazer `fetchSupportBoardMetrics` chamar `fetchSupportBoardMetricsWithIssues` e retornar apenas `{ ...snapshot, alertas: [] }` (alertas preenchidos pelo IPC handler).
  </action>
  <verify>
    grep -n "calcularAlertas" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/external/SupportBoardClient.ts && grep -n "calcularAlertas" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/index.ts
  </verify>
  <acceptance_criteria>
    - `export function calcularAlertas` presente em SupportBoardClient.ts
    - Funcao aceita parametros `(snapshot, history, issues, slaThresholds)` com essa assinatura
    - `calcularAlertas` importado e chamado em index.ts dentro de `fetchAndCacheSustentacao`
    - Cache hit path tambem preenche `alertas` (pode ser com issues=[])
    - Nenhuma chamada existente a `fetchSupportBoardMetrics` quebrada (assinatura publica mantida)
    - `DEFAULT_SLA_DIAS` acessivel dentro de `calcularAlertas` (esta no mesmo arquivo)
    - Build do TypeScript sem erros de tipo: `npx tsc --noEmit` passa
  </acceptance_criteria>
  <done>snapshot retornado pelo IPC contem campo alertas[] com 0 ou mais SustentacaoAlerta; as 4 condicoes estao implementadas; TypeScript compila limpo</done>
</task>

</tasks>

<verification>
```bash
cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit

# Verificar tipos presentes
grep -n "SustentacaoAlerta\|alertas:" src/renderer/src/types/ipc.ts

# Verificar calcularAlertas exportado
grep -n "export function calcularAlertas" src/main/external/SupportBoardClient.ts

# Verificar integracao no IPC
grep -n "calcularAlertas" src/main/index.ts

# Build limpo
npx tsc --noEmit 2>&1 | head -30
```
</verification>

<success_criteria>
- SustentacaoAlerta interface presente em ipc.ts com campos tipo, mensagem, severidade
- SupportBoardSnapshot.alertas campo presente em ipc.ts
- calcularAlertas() exportada em SupportBoardClient.ts implementando 4 condicoes
- index.ts chama calcularAlertas e inclui resultado no snapshot retornado
- TypeScript compila sem erros (npx tsc --noEmit)
- Graceful degradation: history vazio nao causa crash (D-04, D-06 silenciosos)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/999.14-sustentacao-alertas-proativos/999.14-01-SUMMARY.md`
</output>
