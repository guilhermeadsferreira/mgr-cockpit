---
phase: 04-action-system-ux-avancado
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/external/Scheduler.ts
  - src/main/index.ts
autonomous: true
requirements:
  - UX-03
must_haves:
  truths:
    - "A pauta de 1:1 e gerada automaticamente N dias antes do proximo encontro sem o gestor precisar acionar manualmente"
    - "O Scheduler verifica frequencia_1on1_dias e ultimo_1on1 para decidir quando gerar"
  artifacts:
    - path: "src/main/external/Scheduler.ts"
      provides: "Metodo checkAgendaGeneration que gera pautas automaticamente"
      contains: "checkAgendaGeneration"
    - path: "src/main/index.ts"
      provides: "Handler ai:generate-agenda ja existe — Scheduler reutiliza a logica"
      contains: "generate-agenda"
  key_links:
    - from: "src/main/external/Scheduler.ts"
      to: "src/main/index.ts"
      via: "Scheduler chama logica de geracao de pauta internamente (nao via IPC)"
      pattern: "checkAgendaGeneration"
---

<objective>
Implementar agenda generation agendada: Scheduler gera pauta automaticamente N dias antes do proximo 1:1 esperado.

Purpose: O gestor nao precisa lembrar de clicar "Gerar pauta" — o sistema antecipa baseado na frequencia configurada.
Output: Scheduler com checkAgendaGeneration que roda no onAppStart e gera pauta quando proximo 1:1 esta proximo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main/external/Scheduler.ts
@src/main/index.ts
@src/main/registry/PersonRegistry.ts
@src/main/prompts/agenda.prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar checkAgendaGeneration no Scheduler</name>
  <files>src/main/external/Scheduler.ts, src/main/index.ts</files>
  <read_first>
    - src/main/external/Scheduler.ts
    - src/main/index.ts
    - src/main/registry/PersonRegistry.ts
    - src/main/prompts/agenda.prompt.ts
    - src/renderer/src/types/ipc.ts
  </read_first>
  <action>
### Parte A — Logica de geracao automatica no Scheduler

1. Em `src/main/external/Scheduler.ts`, adicionar imports necessarios no topo (verificar quais ja existem):

```typescript
import { ActionRegistry } from '../registry/ActionRegistry'
import { buildAgendaPrompt, renderAgendaMarkdown, type AgendaAIResult } from '../prompts/agenda.prompt'
```

Tambem importar a funcao `runWithProvider` se existir como export do index.ts. Se nao for exportada, usar `ClaudeRunner` diretamente. Verificar no read_first qual abordagem e viavel.

2. Adicionar constante para threshold de antecedencia:
```typescript
const AGENDA_DAYS_BEFORE = 2  // Gerar pauta 2 dias antes do proximo 1:1 esperado
```

3. Adicionar metodo publico `checkAgendaGeneration`:

```typescript
/**
 * Verifica se algum liderado precisa de pauta gerada automaticamente.
 * Criterio: proximo 1:1 esperado em <= AGENDA_DAYS_BEFORE dias e nao ha pauta recente.
 */
async checkAgendaGeneration(): Promise<{ generated: string[] }> {
  const settings = SettingsManager.load()
  const registry = new PersonRegistry(settings.workspacePath)
  const people = registry.list().filter(p => p.relacao === 'liderado')
  const today = new Date()
  const todayStr = today.toISOString().slice(0, 10)
  const generated: string[] = []

  for (const person of people) {
    try {
      // Calcular quando e o proximo 1:1 esperado
      const perfil = registry.getPerfil(person.slug)
      const ultimo1on1 = perfil?.frontmatter?.ultimo_1on1
      if (!ultimo1on1) continue  // Sem historico de 1:1 — nao gerar automaticamente

      const frequenciaDias = person.frequencia_1on1_dias ?? 14
      const ultimoDate = new Date(ultimo1on1)
      const proximoDate = new Date(ultimoDate.getTime() + frequenciaDias * 86_400_000)
      const diasAteProximo = Math.floor((proximoDate.getTime() - today.getTime()) / 86_400_000)

      // Gerar se proximo 1:1 e em <= AGENDA_DAYS_BEFORE dias
      if (diasAteProximo > AGENDA_DAYS_BEFORE) continue
      if (diasAteProximo < -3) continue  // Ja passou muito — nao gerar pauta atrasada

      // Verificar se ja existe pauta recente (ultimos 3 dias)
      const pautas = registry.listPautas(person.slug)
      const pautaRecente = pautas.find(p => {
        const diasSincePauta = Math.floor((today.getTime() - new Date(p.date).getTime()) / 86_400_000)
        return diasSincePauta <= 3
      })
      if (pautaRecente) continue  // Ja tem pauta recente — nao duplicar

      log.info('agenda auto-generation triggered', { slug: person.slug, diasAteProximo, frequenciaDias })

      // Gerar pauta reutilizando a logica existente
      // Emitir evento IPC para que o handler existente processe
      // Alternativa: chamar diretamente (mais autonomo)
      const { BrowserWindow } = await import('electron')
      const win = BrowserWindow.getAllWindows()[0]
      if (win) {
        // Trigger via IPC invoke interno — reutiliza toda a logica do handler existente
        const result = await win.webContents.executeJavaScript(
          `window.api.ai.generateAgenda('${person.slug}')`
        )
        if (result?.success) {
          generated.push(person.slug)
          log.info('pauta auto-gerada', { slug: person.slug })
        }
      }
    } catch (err) {
      log.warn('auto agenda generation falhou', { slug: person.slug, error: err instanceof Error ? err.message : String(err) })
    }
  }

  return { generated }
}
```

NOTA IMPORTANTE: A abordagem via `executeJavaScript` e fragil. A alternativa mais robusta e extrair a logica de geracao de pauta do handler IPC em `index.ts` para uma funcao reutilizavel. Se ao ler `index.ts` voce identificar que a logica esta autocontida no handler, refatorar para funcao exportavel:

```typescript
// Em index.ts, extrair para funcao:
export async function generateAgendaForPerson(slug: string, workspacePath: string): Promise<AgendaResult> {
  // ... logica existente do handler ...
}
```

E no Scheduler chamar diretamente:
```typescript
const { generateAgendaForPerson } = await import('../index')
const result = await generateAgendaForPerson(person.slug, settings.workspacePath)
```

ESCOLHER a abordagem mais limpa apos ler o index.ts. Se refatorar e complexo demais, usar a abordagem via BrowserWindow.

### Parte B — Integrar no onAppStart

4. No metodo `onAppStart()` do Scheduler, APOS o bloco de daily report, adicionar:

```typescript
// Auto-generate agendas for upcoming 1:1s
try {
  const { generated } = await this.checkAgendaGeneration()
  if (generated.length > 0) {
    log.info('pautas auto-geradas no startup', { count: generated.length, people: generated })
  }
} catch (err) {
  log.warn('auto agenda check falhou', { error: err instanceof Error ? err.message : String(err) })
}
```
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Scheduler.ts contains `async checkAgendaGeneration(`
    - Scheduler.ts contains `AGENDA_DAYS_BEFORE`
    - Scheduler.ts contains `diasAteProximo`
    - Scheduler.ts contains `pauta auto-gerada` or `pautas auto-geradas`
    - Scheduler.ts onAppStart contains `checkAgendaGeneration`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Scheduler gera pautas automaticamente 2 dias antes do proximo 1:1 esperado. Nao duplica se pauta recente existe. Roda no startup do app.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compila sem erros
- Scheduler tem checkAgendaGeneration que roda no onAppStart
- Pauta e gerada quando proximo 1:1 em <= 2 dias e nao ha pauta recente
- Falhas sao graceful (nao interrompem startup)
</verification>

<success_criteria>
1. checkAgendaGeneration calcula proximo 1:1 baseado em ultimo_1on1 + frequencia_1on1_dias
2. Pauta gerada automaticamente 2 dias antes sem intervencao manual
3. Nao duplica pauta se ja existe uma nos ultimos 3 dias
</success_criteria>

<output>
After completion, create `.planning/phases/04-action-system-ux-avancado/04-05-SUMMARY.md`
</output>
