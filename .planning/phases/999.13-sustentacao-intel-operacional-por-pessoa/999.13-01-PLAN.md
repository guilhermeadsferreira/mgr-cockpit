---
phase: 999.13-sustentacao-intel-operacional-por-pessoa
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/types/ipc.ts
  - src/main/external/SupportBoardClient.ts
autonomous: true
requirements:
  - INTEL-01
  - INTEL-02

must_haves:
  truths:
    - "SupportBoardSnapshot contém inOutSemanal[] com pelo menos 1 entrada quando há issues nos últimos 90d"
    - "SupportBoardSnapshot contém recorrentesDetectados[] com tipos que aparecem >2x em 30d"
    - "topTipos no snapshot inclui tickets fechados nos últimos 30d além dos abertos"
  artifacts:
    - path: "src/renderer/src/types/ipc.ts"
      provides: "InOutSemanalEntry e RecorrenteDetectado como interfaces exportadas; SupportBoardSnapshot com os novos campos"
      contains: "InOutSemanalEntry"
    - path: "src/main/external/SupportBoardClient.ts"
      provides: "Cálculo de in/out semanal e detecção de recorrentes retornados no snapshot"
      contains: "calcularInOutSemanal"
  key_links:
    - from: "src/main/external/SupportBoardClient.ts"
      to: "src/renderer/src/types/ipc.ts"
      via: "import SupportBoardSnapshot"
      pattern: "inOutSemanal.*InOutSemanalEntry"
---

<objective>
Estender o contrato de dados da sustentação com dois novos campos: `inOutSemanal[]` (vazão semanal de tickets) e `recorrentesDetectados[]` (tipos+labels com >2 ocorrências em 30d). Também enriquecer `topTipos` para incluir tickets fechados, não só abertos.

Purpose: Prover os dados calculados no main process que a UI consumirá no Plan 02. Toda lógica fica no SupportBoardClient — nenhum cálculo no renderer.

Output: Tipos definidos em ipc.ts + cálculos implementados em SupportBoardClient.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Tipos existentes que o executor precisa antes de tocar os arquivos. -->

De src/renderer/src/types/ipc.ts (já existente):
```typescript
export interface SupportBoardSnapshot {
  atualizadoEm: string
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  topTipos: Array<{ tipo: string; count: number }>
  topLabels: Array<{ label: string; count: number }>
  ticketsEmBreach: SupportTicket[]
  porAssignee: Record<string, number>
  complianceRate7d: number | null
  complianceRate30d: number | null
  history: SustentacaoHistoryEntry[]
  // ADICIONAR: inOutSemanal e recorrentesDetectados
}
```

De src/main/external/SupportBoardClient.ts (já existente):
```typescript
// issues: JiraIssue[] — campos disponíveis: key, summary, type, labels, assignee, created, resolved, statusCategory, status
// issues inclui TODOS os tickets dos últimos 90d (abertos + fechados)
// abertos = issues.filter(i => i.statusCategory !== 'done')
// fechados30d = issues filtrados por resolved >= 30d atrás
const THIRTY_DAYS_MS = 30 * 24 * 60 * 60 * 1000
const TOP_N = 5
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar tipos InOutSemanalEntry e RecorrenteDetectado ao ipc.ts</name>
  <files>src/renderer/src/types/ipc.ts</files>

  <read_first>
    - src/renderer/src/types/ipc.ts — ler o arquivo COMPLETO antes de editar; inserir novos tipos após SustentacaoHistoryEntry (linha ~206) e novos campos no SupportBoardSnapshot
  </read_first>

  <action>
Após a interface `SustentacaoHistoryEntry` (após linha 206 aprox.), adicionar as duas novas interfaces:

```typescript
/** Entrada de vazão semanal: tickets abertos (in) vs resolvidos (out) na semana. */
export interface InOutSemanalEntry {
  /** Início da semana no formato YYYY-MM-DD (segunda-feira) */
  semana: string
  /** Tickets criados na semana */
  in: number
  /** Tickets resolvidos na semana */
  out: number
}

/** Tipo de ticket recorrente detectado nos últimos 30 dias (>2 ocorrências). */
export interface RecorrenteDetectado {
  /** Tipo do ticket (ex: "Bug", "Task") */
  tipo: string
  /** Label associado (ex: "auth", "performance") — null se sem label relevante */
  label: string | null
  /** Número de ocorrências nos últimos 30 dias */
  ocorrencias: number
}
```

Em seguida, estender `SupportBoardSnapshot` adicionando ao final da interface (antes do `}`):

```typescript
  /** Vazão semanal: tickets abertos vs resolvidos por semana (últimas 8 semanas) */
  inOutSemanal: InOutSemanalEntry[]
  /** Tipos recorrentes: tipo+label com >2 ocorrências nos últimos 30 dias */
  recorrentesDetectados: RecorrenteDetectado[]
```
  </action>

  <verify>grep -n "InOutSemanalEntry\|RecorrenteDetectado\|inOutSemanal\|recorrentesDetectados" src/renderer/src/types/ipc.ts</verify>

  <acceptance_criteria>
    - `grep "export interface InOutSemanalEntry" src/renderer/src/types/ipc.ts` retorna resultado
    - `grep "export interface RecorrenteDetectado" src/renderer/src/types/ipc.ts` retorna resultado
    - `grep "inOutSemanal: InOutSemanalEntry\[\]" src/renderer/src/types/ipc.ts` retorna resultado
    - `grep "recorrentesDetectados: RecorrenteDetectado\[\]" src/renderer/src/types/ipc.ts` retorna resultado
    - `grep "semana: string" src/renderer/src/types/ipc.ts` retorna resultado
  </acceptance_criteria>

  <done>Dois novos tipos exportados e dois novos campos opcionais presentes em SupportBoardSnapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar calcularInOutSemanal() e detectarRecorrentes() no SupportBoardClient</name>
  <files>src/main/external/SupportBoardClient.ts</files>

  <read_first>
    - src/main/external/SupportBoardClient.ts — ler COMPLETO antes de editar; entender onde `topTipos` é calculado e onde o return do snapshot é montado
    - src/renderer/src/types/ipc.ts — confirmar assinaturas dos novos tipos antes de usar
  </read_first>

  <action>
1. Adicionar import dos novos tipos no topo do arquivo (junto com SupportBoardSnapshot e SupportTicket existentes):
   ```typescript
   import type { SupportBoardSnapshot, SupportTicket, InOutSemanalEntry, RecorrenteDetectado } from '../../renderer/src/types/ipc'
   ```

2. Adicionar constante após `const TOP_N = 5`:
   ```typescript
   const IN_OUT_SEMANAS = 8
   ```

3. Adicionar as duas funções helper antes de `fetchSupportBoardMetrics`:

   ```typescript
   /**
    * Agrupa issues por semana de criação (in) e semana de resolução (out).
    * Retorna últimas N semanas com segunda-feira como chave YYYY-MM-DD.
    */
   function calcularInOutSemanal(
     issues: Array<{ created: string; resolved: string | null; statusCategory: string }>,
     semanas: number
   ): InOutSemanalEntry[] {
     // Gerar array de segundas-feiras das últimas N semanas
     const result: InOutSemanalEntry[] = []
     const now = new Date()
     // Retroceder ao último segunda-feira
     const dayOfWeek = now.getDay() // 0=domingo, 1=segunda...
     const daysToMonday = dayOfWeek === 0 ? 6 : dayOfWeek - 1
     const thisMonday = new Date(now)
     thisMonday.setHours(0, 0, 0, 0)
     thisMonday.setDate(now.getDate() - daysToMonday)
   
     for (let i = semanas - 1; i >= 0; i--) {
       const weekStart = new Date(thisMonday)
       weekStart.setDate(thisMonday.getDate() - i * 7)
       const weekEnd = new Date(weekStart)
       weekEnd.setDate(weekStart.getDate() + 7)
   
       const semanaKey = weekStart.toISOString().slice(0, 10)
   
       const inCount = issues.filter((issue) => {
         const t = new Date(issue.created).getTime()
         return t >= weekStart.getTime() && t < weekEnd.getTime()
       }).length
   
       const outCount = issues.filter((issue) => {
         if (!issue.resolved || issue.statusCategory !== 'done') return false
         const t = new Date(issue.resolved).getTime()
         return t >= weekStart.getTime() && t < weekEnd.getTime()
       }).length
   
       result.push({ semana: semanaKey, in: inCount, out: outCount })
     }
   
     return result
   }
   
   /**
    * Detecta tipos+labels recorrentes (>2 ocorrências nos últimos 30 dias).
    * Considera todos os tickets (abertos e fechados) criados nos últimos 30d.
    * Combina tipo e primeira label; se sem label, usa null.
    */
   function detectarRecorrentes(
     issues: Array<{ type: string; labels: string[]; created: string }>,
     windowDias: number
   ): RecorrenteDetectado[] {
     const cutoff = Date.now() - windowDias * 24 * 60 * 60 * 1000
     const recent = issues.filter((i) => new Date(i.created).getTime() >= cutoff)
   
     // Chave: "tipo||label" (usa primeira label ou "__none__")
     const counts: Record<string, { tipo: string; label: string | null; ocorrencias: number }> = {}
   
     for (const issue of recent) {
       const label = issue.labels.length > 0 ? issue.labels[0] : null
       const chave = `${issue.type}||${label ?? '__none__'}`
       if (!counts[chave]) {
         counts[chave] = { tipo: issue.type, label, ocorrencias: 0 }
       }
       counts[chave].ocorrencias++
     }
   
     return Object.values(counts)
       .filter((r) => r.ocorrencias > 2)
       .sort((a, b) => b.ocorrencias - a.ocorrencias)
   }
   ```

4. Modificar o cálculo de `tipoCounts` existente para incluir tickets fechados dos últimos 30d (além dos abertos). Atualmente itera sobre `abertos` apenas. Mudar para:
   ```typescript
   // Contagens por tipo e label — abertos + fechados nos últimos 30d
   const ticketsParaContagem = [...abertos, ...fechados30d]
   for (const issue of ticketsParaContagem) {
     tipoCounts[issue.type] = (tipoCounts[issue.type] ?? 0) + 1
     // manter labelCounts e assigneeCounts apenas sobre abertos:
   }
   // labelCounts e assigneeCounts continuam sobre `abertos` apenas (não mudar)
   ```
   Atenção: separar o loop — `tipoCounts` usa `ticketsParaContagem`, `labelCounts` e `assigneeCounts` continuam usando `abertos`. Reorganizar o loop existente em dois loops ou adicionar tipoCounts separado.

5. Calcular os novos dados antes do `return`:
   ```typescript
   const inOutSemanal = calcularInOutSemanal(issues, IN_OUT_SEMANAS)
   const recorrentesDetectados = detectarRecorrentes(issues, 30)
   ```

6. Adicionar os novos campos ao objeto retornado:
   ```typescript
   return {
     // ...campos existentes...
     inOutSemanal,
     recorrentesDetectados,
   }
   ```
  </action>

  <verify>npx tsc --noEmit 2>&1 | head -30</verify>

  <acceptance_criteria>
    - `grep "calcularInOutSemanal" src/main/external/SupportBoardClient.ts` retorna resultado
    - `grep "detectarRecorrentes" src/main/external/SupportBoardClient.ts` retorna resultado
    - `grep "inOutSemanal," src/main/external/SupportBoardClient.ts` retorna resultado (no return do snapshot)
    - `grep "recorrentesDetectados," src/main/external/SupportBoardClient.ts` retorna resultado (no return do snapshot)
    - `grep "InOutSemanalEntry\|RecorrenteDetectado" src/main/external/SupportBoardClient.ts` retorna resultado (imports)
    - `npx tsc --noEmit` termina sem erros de tipo relacionados a SupportBoardSnapshot ou InOutSemanalEntry
  </acceptance_criteria>

  <done>fetchSupportBoardMetrics retorna snapshot com inOutSemanal[] (8 semanas) e recorrentesDetectados[] (>2x em 30d). TypeScript compila sem erros.</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit 2>&1 | grep -E "ipc\.ts|SupportBoardClient|InOutSemanal|Recorrente"
grep -n "inOutSemanal\|recorrentesDetectados" src/renderer/src/types/ipc.ts src/main/external/SupportBoardClient.ts
```
</verification>

<success_criteria>
- SupportBoardSnapshot tem campos `inOutSemanal: InOutSemanalEntry[]` e `recorrentesDetectados: RecorrenteDetectado[]`
- SupportBoardClient computa e retorna ambos os campos
- TypeScript compila sem erros
- topTipos inclui tickets fechados nos últimos 30d (além dos abertos)
</success_criteria>

<output>
After completion, create `.planning/phases/999.13-sustentacao-intel-operacional-por-pessoa/999.13-01-SUMMARY.md`
</output>
