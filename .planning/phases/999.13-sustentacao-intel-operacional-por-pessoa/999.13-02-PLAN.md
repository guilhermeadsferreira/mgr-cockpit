---
phase: 999.13-sustentacao-intel-operacional-por-pessoa
plan: "02"
type: execute
wave: 2
depends_on:
  - "999.13-01"
files_modified:
  - src/renderer/src/views/SustentacaoView.tsx
autonomous: true
requirements:
  - INTEL-03
  - INTEL-04

must_haves:
  truths:
    - "SustentacaoView exibe seção 'Inteligência Operacional' abaixo dos SLA Compliance cards"
    - "Seção contém gráfico in/out semanal (barras SVG ou linhas) com 8 semanas"
    - "Seção contém curva histórica de backlog (ticketsAbertos por dia) a partir do history[]"
    - "Seção contém ranking de tipos por frequência incluindo tickets fechados"
    - "Recorrentes com >2 ocorrências exibidos com aviso visual de candidato a raiz"
    - "Seção não aparece quando inOutSemanal está vazio (graceful degradation)"
  artifacts:
    - path: "src/renderer/src/views/SustentacaoView.tsx"
      provides: "Seção IntelOperacional com 4 sub-blocos: in/out, backlog, top tipos, recorrentes"
      contains: "Inteligência Operacional"
  key_links:
    - from: "src/renderer/src/views/SustentacaoView.tsx"
      to: "src/renderer/src/types/ipc.ts"
      via: "import InOutSemanalEntry, RecorrenteDetectado"
      pattern: "InOutSemanalEntry|RecorrenteDetectado"
---

<objective>
Adicionar a seção "Inteligência Operacional" na SustentacaoView com quatro sub-blocos: (1) gráfico de vazão in/out semanal, (2) curva histórica de backlog, (3) top tipos por frequência expandido, (4) lista de recorrentes detectados com alerta visual.

Purpose: Dar ao gestor visibilidade de tendência e padrões operacionais que o snapshot instantâneo não oferece — "estou ganhando ou perdendo terreno?" e "o que dói mais?".

Output: SustentacaoView.tsx com nova seção renderizada abaixo dos SLA Compliance cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/999.13-sustentacao-intel-operacional-por-pessoa/999.13-01-SUMMARY.md

<interfaces>
<!-- Tipos adicionados no Plan 01 — o executor usa diretamente. -->

```typescript
// De src/renderer/src/types/ipc.ts (adicionados no Plan 01)
export interface InOutSemanalEntry {
  semana: string   // YYYY-MM-DD (segunda-feira)
  in: number       // tickets criados na semana
  out: number      // tickets resolvidos na semana
}

export interface RecorrenteDetectado {
  tipo: string
  label: string | null
  ocorrencias: number
}

// Em SupportBoardSnapshot (adicionados no Plan 01):
inOutSemanal: InOutSemanalEntry[]
recorrentesDetectados: RecorrenteDetectado[]

// Em SustentacaoHistoryEntry (já existia desde 999.11):
interface SustentacaoHistoryEntry {
  date: string       // YYYY-MM-DD
  fetchedAt: number
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  breachCount: number
  complianceRate7d: number | null
  complianceRate30d: number | null
}
```

Padrões visuais do SustentacaoView.tsx existente:
- `Section` component: `<Section title="...">children</Section>` — usar para todos os sub-blocos
- `MiniLineChart` component: `<MiniLineChart points={number[]} color="var(--accent)" />` — reusar para curva de backlog e in/out
- CSS variables: `var(--surface)`, `var(--border)`, `var(--accent)`, `var(--red)`, `var(--text-primary)`, `var(--text-muted)`, `var(--text-secondary)`, `var(--font)`, `var(--font-mono)`, `var(--surface-2)`, `var(--border-subtle)`
- Sem biblioteca de gráficos externa — SVG inline (padrão estabelecido em 999.11)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar seção Inteligência Operacional na SustentacaoView</name>
  <files>src/renderer/src/views/SustentacaoView.tsx</files>

  <read_first>
    - src/renderer/src/views/SustentacaoView.tsx — ler COMPLETO antes de editar; entender onde inserir a nova seção (após o bloco "Evolução (últimos 30 dias)" e antes de "Análise de IA")
    - src/renderer/src/types/ipc.ts — confirmar nomes exatos dos campos inOutSemanal, recorrentesDetectados, InOutSemanalEntry, RecorrenteDetectado
    - .planning/phases/999.13-sustentacao-intel-operacional-por-pessoa/999.13-01-SUMMARY.md — confirmar quais campos foram adicionados no Plan 01
  </read_first>

  <action>
1. Adicionar ao import dos tipos no topo do arquivo:
   ```typescript
   import type { SupportBoardSnapshot, SustentacaoHistoryEntry, InOutSemanalEntry, RecorrenteDetectado } from '../types/ipc'
   ```

2. Adicionar o componente `InOutBarChart` após `MiniLineChart` (antes de `SustentacaoView`):
   ```typescript
   /**
    * Gráfico de barras in/out semanal com SVG inline.
    * Barras azuis (accent) = in, barras verdes mais claras = out.
    */
   function InOutBarChart({
     entries,
     width = 420,
     height = 60,
   }: {
     entries: InOutSemanalEntry[]
     width?: number
     height?: number
   }) {
     if (entries.length === 0) return null
     const maxVal = Math.max(...entries.flatMap((e) => [e.in, e.out]), 1)
     const barGroupWidth = width / entries.length
     const barWidth = Math.max(4, barGroupWidth * 0.35)
     const gap = 2
   
     return (
       <svg width={width} height={height} style={{ overflow: 'visible', display: 'block' }}>
         {entries.map((entry, i) => {
           const cx = (i / entries.length) * width + barGroupWidth / 2
           const inH = Math.max(2, (entry.in / maxVal) * (height - 4))
           const outH = Math.max(2, (entry.out / maxVal) * (height - 4))
           return (
             <g key={entry.semana}>
               {/* Barra "in" (tickets criados) — à esquerda */}
               <rect
                 x={cx - barWidth - gap / 2}
                 y={height - inH}
                 width={barWidth}
                 height={inH}
                 fill="var(--accent)"
                 opacity={0.85}
                 rx={1}
               />
               {/* Barra "out" (tickets resolvidos) — à direita */}
               <rect
                 x={cx + gap / 2}
                 y={height - outH}
                 width={barWidth}
                 height={outH}
                 fill="var(--text-muted)"
                 opacity={0.6}
                 rx={1}
               />
             </g>
           )
         })}
       </svg>
     )
   }
   ```

3. Adicionar o componente `IntelOperacionalSection` após `InOutBarChart` (antes de `SustentacaoView`):
   ```typescript
   function IntelOperacionalSection({
     snapshot,
   }: {
     snapshot: SupportBoardSnapshot
   }) {
     const { inOutSemanal, recorrentesDetectados, topTipos, history } = snapshot
   
     // Curva de backlog: ticketsAbertos por dia (do history)
     const backlogPoints = history.slice(-30).map((e) => e.ticketsAbertos)
   
     const hasData = inOutSemanal.length > 0 || backlogPoints.length >= 2
     if (!hasData) return null
   
     return (
       <Section title="Inteligência Operacional">
         {/* Sub-bloco 1: Vazão in/out semanal */}
         {inOutSemanal.length > 0 && (
           <div style={{ marginBottom: 24 }}>
             <div style={{
               fontSize: 12, fontWeight: 600, color: 'var(--text-secondary)',
               fontFamily: 'var(--font)', marginBottom: 10,
             }}>
               Vazão semanal (últimas 8 semanas)
             </div>
             <InOutBarChart entries={inOutSemanal} />
             <div style={{
               display: 'flex', gap: 16, marginTop: 8,
               fontSize: 11.5, color: 'var(--text-muted)', fontFamily: 'var(--font)',
             }}>
               <span style={{ display: 'flex', alignItems: 'center', gap: 4 }}>
                 <span style={{ width: 10, height: 10, borderRadius: 2, background: 'var(--accent)', display: 'inline-block' }} />
                 Abertos (in)
               </span>
               <span style={{ display: 'flex', alignItems: 'center', gap: 4 }}>
                 <span style={{ width: 10, height: 10, borderRadius: 2, background: 'var(--text-muted)', opacity: 0.6, display: 'inline-block' }} />
                 Resolvidos (out)
               </span>
             </div>
           </div>
         )}
   
         {/* Sub-bloco 2: Curva histórica de backlog */}
         {backlogPoints.length >= 2 && (
           <div style={{ marginBottom: 24 }}>
             <div style={{
               fontSize: 12, fontWeight: 600, color: 'var(--text-secondary)',
               fontFamily: 'var(--font)', marginBottom: 10,
             }}>
               Backlog histórico (tickets abertos)
             </div>
             <MiniLineChart points={backlogPoints} width={420} height={50} color="var(--accent)" />
           </div>
         )}
   
         {/* Sub-bloco 3: Top tipos (abertos + fechados 30d) */}
         {topTipos.length > 0 && (
           <div style={{ marginBottom: 24 }}>
             <div style={{
               fontSize: 12, fontWeight: 600, color: 'var(--text-secondary)',
               fontFamily: 'var(--font)', marginBottom: 10,
             }}>
               Tipos mais frequentes (abertos + fechados 30d)
             </div>
             <div style={{ display: 'flex', flexWrap: 'wrap', gap: 8 }}>
               {topTipos.map((item) => (
                 <span key={item.tipo} style={{
                   padding: '4px 12px', borderRadius: 20,
                   background: 'var(--surface-2)', border: '1px solid var(--border)',
                   fontSize: 12.5, color: 'var(--text-secondary)', fontFamily: 'var(--font)',
                 }}>
                   {item.tipo}: <strong style={{ color: 'var(--text-primary)' }}>{item.count}</strong>
                 </span>
               ))}
             </div>
           </div>
         )}
   
         {/* Sub-bloco 4: Recorrentes detectados */}
         {recorrentesDetectados.length > 0 && (
           <div>
             <div style={{
               fontSize: 12, fontWeight: 600, color: 'var(--text-secondary)',
               fontFamily: 'var(--font)', marginBottom: 10,
             }}>
               Candidatos a resolver na raiz (últimos 30d)
             </div>
             <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
               {recorrentesDetectados.map((r, i) => (
                 <div key={i} style={{
                   display: 'flex', alignItems: 'center', gap: 10,
                   padding: '8px 12px', borderRadius: 6,
                   background: 'rgba(184, 64, 64, 0.06)',
                   border: '1px solid rgba(184, 64, 64, 0.2)',
                 }}>
                   <span style={{ fontSize: 13 }}>⚠️</span>
                   <span style={{
                     fontSize: 12.5, color: 'var(--text-primary)', fontFamily: 'var(--font)',
                     flex: 1,
                   }}>
                     <strong>{r.tipo}</strong>
                     {r.label && <span style={{ color: 'var(--text-muted)' }}> · {r.label}</span>}
                   </span>
                   <span style={{
                     fontSize: 12, color: 'var(--red)', fontFamily: 'var(--font-mono)',
                     fontWeight: 600,
                   }}>
                     {r.ocorrencias}x em 30d
                   </span>
                 </div>
               ))}
             </div>
           </div>
         )}
       </Section>
     )
   }
   ```

4. No JSX do `SustentacaoView`, inserir `<IntelOperacionalSection snapshot={snapshot} />` após o bloco "Evolução (últimos 30 dias)" existente e antes do bloco "Análise de IA":
   ```tsx
   {/* Evolução (últimos 30 dias) — bloco existente */}
   {snapshot.history.length >= 2 && (
     <Section title="Evolução (últimos 30 dias)">
       ...
     </Section>
   )}

   {/* Inteligência Operacional — NOVO */}
   <IntelOperacionalSection snapshot={snapshot} />

   {/* Análise de IA — bloco existente */}
   {analysisResult && (
     <Section title="Análise de IA">
       ...
     </Section>
   )}
   ```

5. Remover a seção "Distribuição por Tipo" existente (a que exibe `snapshot.topTipos` com chips) pois agora é renderizada dentro de `IntelOperacionalSection`. Isso evita duplicação.

   Localizar e remover o bloco:
   ```tsx
   {/* Distribuição por Tipo */}
   {snapshot.topTipos.length > 0 && (
     <Section title="Distribuição por Tipo">
       ...
     </Section>
   )}
   ```
  </action>

  <verify>npx tsc --noEmit 2>&1 | head -30</verify>

  <acceptance_criteria>
    - `grep "Inteligência Operacional" src/renderer/src/views/SustentacaoView.tsx` retorna resultado
    - `grep "IntelOperacionalSection" src/renderer/src/views/SustentacaoView.tsx` retorna 2+ resultados (definição + uso no JSX)
    - `grep "InOutBarChart" src/renderer/src/views/SustentacaoView.tsx` retorna 2+ resultados (definição + uso)
    - `grep "Candidatos a resolver na raiz" src/renderer/src/views/SustentacaoView.tsx` retorna resultado
    - `grep "Vazão semanal" src/renderer/src/views/SustentacaoView.tsx` retorna resultado
    - `grep "Backlog histórico" src/renderer/src/views/SustentacaoView.tsx` retorna resultado
    - `grep "import type.*InOutSemanalEntry.*RecorrenteDetectado" src/renderer/src/views/SustentacaoView.tsx` retorna resultado (ou os tipos no mesmo import existente)
    - `npx tsc --noEmit` termina sem erros relacionados a SustentacaoView ou InOutSemanalEntry
    - A seção "Distribuição por Tipo" standalone foi removida (não duplicada com IntelOperacionalSection)
  </acceptance_criteria>

  <done>SustentacaoView renderiza seção "Inteligência Operacional" com os 4 sub-blocos. TypeScript compila sem erros. Nenhuma duplicação de topTipos.</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit 2>&1 | grep -E "SustentacaoView|InOutSemanal|Recorrente|ipc\.ts"
grep -n "IntelOperacionalSection\|InOutBarChart\|Candidatos a resolver" src/renderer/src/views/SustentacaoView.tsx
```
</verification>

<success_criteria>
- SustentacaoView tem seção "Inteligência Operacional" inserida entre "Evolução" e "Análise de IA"
- Gráfico in/out semanal (InOutBarChart SVG) com legenda in/out
- Curva histórica de backlog (MiniLineChart reutilizado)
- Top tipos expandido (abertos + fechados 30d) dentro da seção
- Recorrentes com aviso ⚠️ e contagem "Nx em 30d"
- Seção não renderiza quando não há dados (graceful degradation)
- TypeScript compila sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/999.13-sustentacao-intel-operacional-por-pessoa/999.13-02-SUMMARY.md`
</output>
