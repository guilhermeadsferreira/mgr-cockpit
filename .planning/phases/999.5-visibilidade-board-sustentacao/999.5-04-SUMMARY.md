---
phase: 999.5-visibilidade-board-sustentacao
plan: "04"
subsystem: ipc-sustentacao
tags: [ipc, sustentacao, cache, preload]
dependency_graph:
  requires: [999.5-01, 999.5-02]
  provides: [sustentacao-ipc-handlers, window.api.sustentacao]
  affects: [src/main/index.ts, src/preload/index.ts]
tech_stack:
  added: []
  patterns: [ExternalDataPass cache pattern (cacheDir + TTL), private helper function para evitar duplicacao de handlers]
key_files:
  created: []
  modified:
    - src/main/index.ts
    - src/preload/index.ts
decisions:
  - fetchAndCacheSustentacao extraída como função privada para que sustentacao:refresh possa reutilizar sem duplicar lógica de fetch e cache
  - cache gravado em workspacePath/../cache/sustentacao/board.json seguindo padrão ExternalDataPass mas com diretório dedicado ao módulo
  - runAnalysis adicionado no preload prevendo Plan 06 (prompt IA) — handler será criado naquele plan
metrics:
  duration: "~2min"
  completed: "2026-04-01"
  tasks_completed: 2
  files_modified: 2
---

# Phase 999.5 Plan 04: IPC Handlers Sustentacao Summary

IPC handlers sustentacao:get-data e sustentacao:refresh registrados em index.ts com cache em disco (1h TTL) e namespace window.api.sustentacao exposto no preload.

## What Was Built

### Task 1: IPC handlers em index.ts

Adicionados dois imports no topo de `src/main/index.ts`:
- `fetchSupportBoardMetrics` de `./external/SupportBoardClient`
- `SupportBoardSnapshot` de `../renderer/src/types/ipc`

Criada função privada `fetchAndCacheSustentacao()` com:
- Guard clause: retorna `null` quando `jiraSupportProjectKey`, `jiraBaseUrl`, `jiraEmail` ou `jiraApiToken` não configurados
- Cache em disco: `workspacePath/../cache/sustentacao/board.json` com TTL de 1h
- Leitura de cache com try/catch (cache inválido -> refetch silencioso)
- Escrita de cache com try/catch (falha não impede retorno dos dados)

Handler `sustentacao:get-data`: delega para `fetchAndCacheSustentacao()`.

Handler `sustentacao:refresh`: invalida cache gravando `{ data: null, fetchedAt: 0 }` e chama `fetchAndCacheSustentacao()`.

### Task 2: Namespace no preload

Namespace `sustentacao` adicionado ao objeto do `contextBridge.exposeInMainWorld('api', {...})` em `src/preload/index.ts`:
- `getData()` → `ipcRenderer.invoke('sustentacao:get-data')`
- `refresh()` → `ipcRenderer.invoke('sustentacao:refresh')`
- `runAnalysis()` → `ipcRenderer.invoke('sustentacao:run-analysis')` (Plan 06)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Padrão de reuso do handler refresh**

- **Found during:** Task 1
- **Issue:** O plano sugeria usar `ipcMain.emit('sustentacao:get-data', null)` para delegar do refresh ao get-data, mas este padrão não funciona corretamente para handlers registrados com `ipcMain.handle` (emit é para eventos, não para handlers de IPC invoke).
- **Fix:** Extraída função privada `fetchAndCacheSustentacao()` que ambos os handlers chamam, eliminando a dependência de emit e garantindo comportamento correto.
- **Files modified:** `src/main/index.ts`
- **Commit:** 4a56a09

## Known Stubs

None — os IPC handlers estão completamente implementados. O `runAnalysis` no preload invoca um channel que ainda não tem handler registrado (será criado no Plan 06), mas isso não bloqueia a funcionalidade deste plan.

## Self-Check: PASSED

- src/main/index.ts existe e contém handlers: FOUND
- src/preload/index.ts existe e contém namespace: FOUND
- Commit 4a56a09: FOUND
- Commit d3b4231: FOUND
