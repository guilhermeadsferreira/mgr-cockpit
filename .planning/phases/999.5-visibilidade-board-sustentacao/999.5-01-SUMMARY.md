---
phase: 999.5-visibilidade-board-sustentacao
plan: "01"
subsystem: types
tags: [types, ipc, settings, router, sustentacao]
dependency_graph:
  requires: []
  provides:
    - SupportTicket (ipc.ts)
    - SupportBoardSnapshot (ipc.ts)
    - AppSettings.jiraSupportBoardId (ipc.ts + SettingsManager.ts)
    - AppSettings.jiraSupportProjectKey (ipc.ts + SettingsManager.ts)
    - AppSettings.jiraSlaThresholds (ipc.ts + SettingsManager.ts)
    - ViewName 'sustentacao' (router.tsx)
  affects:
    - Todos os planos subsequentes da phase 999.5
tech_stack:
  added: []
  patterns:
    - Campos opcionais em AppSettings para backward-compatibility
    - AppSettings espelhada em ipc.ts (renderer) e SettingsManager.ts (main)
key_files:
  created: []
  modified:
    - src/renderer/src/types/ipc.ts
    - src/main/registry/SettingsManager.ts
    - src/renderer/src/router.tsx
decisions:
  - Campos opcionais sem alterar DEFAULTS em SettingsManager — settings existentes permanecem validos
  - SupportTicket e SupportBoardSnapshot inseridos entre ExternalDataSnapshot e ExternalHistoricoEntry — agrupa tipos external intelligence
  - 'sustentacao' adicionado apos 'audit' no ViewName — mantem ordem existente intacta
metrics:
  duration: 67s
  completed: "2026-04-02"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 3
---

# Phase 999.5 Plan 01: Contratos de Tipos Sustentacao Summary

Tipos IPC SupportTicket + SupportBoardSnapshot exportados, AppSettings estendido com campos jiraSupportBoardId/jiraSupportProjectKey/jiraSlaThresholds em ambos os arquivos, e ViewName inclui 'sustentacao' — fundacao de tipos para fase visibilidade-board-sustentacao.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Adicionar SupportTicket e SupportBoardSnapshot em ipc.ts e novos campos AppSettings | 51b4299 | src/renderer/src/types/ipc.ts |
| 2 | Espelhar novos campos AppSettings em SettingsManager.ts e adicionar 'sustentacao' ao ViewName | 93a53e6 | src/main/registry/SettingsManager.ts, src/renderer/src/router.tsx |

## What Was Built

- **SupportTicket** — interface com key, summary, type, labels, assignee, ageDias, status, slaBreached, recentComments
- **SupportBoardSnapshot** — interface com ticketsAbertos, ticketsFechadosUltimos30d, topTipos, topLabels, ticketsEmBreach, porAssignee
- **AppSettings** estendida com campos de sustentacao em `ipc.ts` (renderer) e `SettingsManager.ts` (main process)
- **ViewName** inclui `'sustentacao'` — navigate('sustentacao') compila sem erro TypeScript

## Verification

- `grep -rn "SupportBoardSnapshot" src/renderer/src/types/ipc.ts` — retorna match na linha 170
- `grep -rn "jiraSupportBoardId" src/main/registry/SettingsManager.ts src/renderer/src/types/ipc.ts` — retorna matches em ambos
- `grep -n "sustentacao" src/renderer/src/router.tsx` — retorna `| 'sustentacao'` na linha 17
- `npx tsc --noEmit` — zero erros

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None — este plano define apenas contratos de tipos. Nenhum dado flui para UI neste plano.

## Self-Check: PASSED

- src/renderer/src/types/ipc.ts — FOUND (modified, contains SupportBoardSnapshot)
- src/main/registry/SettingsManager.ts — FOUND (modified, contains jiraSupportBoardId)
- src/renderer/src/router.tsx — FOUND (modified, contains 'sustentacao')
- Commit 51b4299 — FOUND
- Commit 93a53e6 — FOUND
